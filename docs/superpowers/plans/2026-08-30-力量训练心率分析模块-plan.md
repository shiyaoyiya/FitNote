# 力量训练心率分析模块 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 废止训练页运行时的 Keytel/MET 热量估算与累计时长统计（存储字段保留兼容），新建 Mechanical/ Cardiovascular 双指标分离的力量训练分析能力，含训练分析折叠卡片、3 Tab 独立分析页、动作 HR Profile 长期画像，并支持 restingHR 档案与 Epley e1RM 估算（全量 estimated 标记，不捏造 HR 数据，不用 RIR）。

**Architecture:** 增量渐进式（方案一）：底层纯算法 strengthTrainingAnalyzer.js 独立 TDD 验证 → 数据扩展 dayHelper(+timestamp) / userProfile(+restingHR) → 独立 UI 组件先行 → 最后 day.vue 一处集成（移除 kcal 逻辑 + 接入 hrSamplesWithTs + TrainingAnalysisCard + 分析调用）。风险：每个 task 独立可验收，核心算法不与 UI 耦合，随时可回滚。

**Tech Stack:** uni-app (Vue 3 Options/Composition 混用) / Pinia / 微信小程序+Android App / 蓝牙 HRS（已有 bleHeartRate 保持不变）/ 自写 `node tests/*.test.js` 纯函数测试 runner（对齐 tests/importIntegration.test.js 风格）/ canvas 自制图表（不引入 Chart.js）。

**Spec:** [docs/superpowers/specs/2026-08-30-力量训练心率分析模块-design.md](file:///d:/小程序/FitNote/docs/superpowers/specs/2026-08-30-力量训练心率分析模块-design.md)

**测试约定:** 纯函数模块 → `node tests/<file>.test.js`（`import assert from 'assert'` + 自写 `test()` wrapper）；UI/页面 → 微信开发者工具 + Android 真机（每个 task 末尾写手动验证预期）。

---

## Task 1: dayHelper — entry 新增 timestamp（兼容旧 entry=null）

**Files:**
- Modify: `utils/dayHelper.js`（`buildEntry` 返回值 + timestamp / `normalizeEntry` 兜底 null / `buildEmptyEntry` 可选同步）
- Test: `tests/dayHelperTimestamp.test.js`

- [ ] **Step 1: 写失败测试**

```javascript
// tests/dayHelperTimestamp.test.js
import assert from 'assert'
import { buildEntry, normalizeEntry, ENTRY_TYPE } from '../utils/dayHelper.js'

let passed = 0, failed = 0
function test(name, fn) {
  try { fn(); console.log(`✓ ${name}`); passed++ }
  catch (e) { console.log(`✗ ${name}\n  ${e.message}`); failed++ }
}

test('buildEntry("10×50") 返回带 timestamp 数字（毫秒级 Date.now() 范围）的 Entry', () => {
  const entry = buildEntry('10×50')
  assert.ok(entry, 'entry 不为 null')
  assert.ok(typeof entry.timestamp === 'number', 'timestamp 是 number')
  const now = Date.now()
  assert.ok(entry.timestamp <= now + 1000, 'timestamp <= now+1s')
  assert.ok(entry.timestamp >= now - 5000, 'timestamp >= now-5s')
})

test('buildEntry("10×20;8×20") 复合组也带 timestamp', () => {
  const entry = buildEntry('10×20;8×20')
  assert.strictEqual(entry.type, ENTRY_TYPE.COMPOSITE)
  assert.ok(typeof entry.timestamp === 'number')
})

test('normalizeEntry 处理旧 entry（无 timestamp 字段）→ 填 null，不填假值', () => {
  const raw = { input: '10×30', total: 300, type: 'normal', stages: [{ reps: 10, weight: 30, total: 300 }] }
  const normalized = normalizeEntry(raw)
  assert.strictEqual(normalized.timestamp, null,
    '旧 entry 没 timestamp 时 normalize 后必须是 null（不伪造 0 或 Date.now）')
})

test('normalizeEntry 处理新 entry（已有 timestamp）→ 原样保留 timestamp', () => {
  const raw = { input: '10×30', total: 300, type: 'normal', stages: [{ reps: 10, weight: 30, total: 300 }], timestamp: 1700000000000 }
  const normalized = normalizeEntry(raw)
  assert.strictEqual(normalized.timestamp, 1700000000000)
})

console.log(`\n${passed} passed, ${failed} failed`)
process.exit(failed ? 1 : 0)
```

- [ ] **Step 2: 跑测试验证失败**

Run: `node tests/dayHelperTimestamp.test.js`
Expected: FAIL（`entry.timestamp === undefined` 导致 `typeof` check 失败或 normalizeEntry 返回 undefined）

- [ ] **Step 3: 修改 utils/dayHelper.js buildEntry 返回对象末尾加 timestamp**

在 `buildEntry` 最终 return 对象中新增最后一项：
```javascript
  return {
    input: inputStr,
    total,
    type: type || ENTRY_TYPE.NORMAL,
    stages: builtStages,
    timestamp: Date.now(),            // ← 新增：保存组确认时的毫秒时间戳
    ...(bwMode ? { bwMode } : {}),
  }
```

在 `normalizeEntry` 末尾（返回前）新增兜底：
```javascript
  // 兼容旧 dayData：entry 无 timestamp 字段时显式置 null（不填假值）
  if (!('timestamp' in n)) n.timestamp = null
  return n
```

如果 `buildEmptyEntry` 存在（创建空 entry 占位模板），在其中同样加 `timestamp: null`（占位组无保存时间点）。

- [ ] **Step 4: 跑测试验证通过**

Run: `node tests/dayHelperTimestamp.test.js`
Expected: 4/4 passed

- [ ] **Step 5: Commit**

```bash
git add utils/dayHelper.js tests/dayHelperTimestamp.test.js
git commit -m "feat(analysis): entry buildEntry/normalizeEntry add timestamp field for per-set HR time windows"
```

---

## Task 2: userProfile store + BodyProfilePopup 扩展 restingHR（数据侧 store 部分；Task 7 再做 UI）

**Files:**
- Modify: `stores/userProfile.js`（state/load/save/toProfile）
- Test: `tests/userProfileRestingHr.test.js`

- [ ] **Step 1: 写失败测试**

```javascript
// tests/userProfileRestingHr.test.js
import assert from 'assert'
import { createPinia, setActivePinia } from 'pinia'
import { useUserProfile } from '../stores/userProfile.js'

// 最小 Pinia 环境（node 下跑，无需 uni）
setActivePinia(createPinia())
const up = useUserProfile()

let passed = 0, failed = 0
function test(name, fn) {
  try { fn(); console.log(`✓ ${name}`); passed++ }
  catch (e) { console.log(`✗ ${name}\n  ${e.message}`); failed++ }
}

test('初始 state.restingHR === null（默认未填）', () => {
  assert.strictEqual(up.restingHR, null)
})

test('setProfile 写入 restingHR=65 → state 同步', () => {
  up.setProfile({ restingHR: 65 })
  assert.strictEqual(up.restingHR, 65)
})

test('setProfile 写入 restingHR=25（<30 越界）→ 强制 null', () => {
  up.setProfile({ restingHR: 25 })
  assert.strictEqual(up.restingHR, null, '越界<30 应为 null 拒绝')
})

test('setProfile 写入 restingHR=150（>120 越界）→ 强制 null', () => {
  up.setProfile({ restingHR: 150 })
  assert.strictEqual(up.restingHR, null)
})

test('toProfile() 返回值包含 restingHR 字段，与 state 同步', () => {
  up.setProfile({ restingHR: 70 })
  const p = up.toProfile()
  assert.strictEqual(p.restingHR, 70)
})

test('load() 反序列化包含 restingHR=null 不崩', () => {
  up.load({ gender: 'male', age: 28, height: 180, restingHR: null })
  assert.strictEqual(up.restingHR, null)
})

console.log(`\n${passed} passed, ${failed} failed`)
process.exit(failed ? 1 : 0)
```

- [ ] **Step 2: 跑测试验证失败**

Run: `node tests/userProfileRestingHr.test.js`
Expected: FAIL（`up.restingHR` 不存在或 setProfile 不认 restingHR）

- [ ] **Step 3: 修改 stores/userProfile.js**

3a. state 默认值加 `restingHR: null`。

3b. `setProfile(p)` 内，对 `p.restingHR` 做边界校验：
```javascript
if (p.restingHR !== undefined) {
  const v = Number(p.restingHR)
  // 清空 或 NaN 或 越界 → null
  if (p.restingHR === '' || p.restingHR === null || Number.isNaN(v)) state.restingHR = null
  else state.restingHR = (v >= 30 && v <= 120) ? v : null
}
```

3c. `load(raw)` 中同样逻辑读取 `raw.restingHR`。

3d. `toProfile()` 返回对象加 `restingHR: state.restingHR`。

3e. `save()` / `persist` 序列化确保 restingHR 持久化（Pinia 若已有 `$subscribe` + JSON.stringify state，则无需额外处理）。

- [ ] **Step 4: 跑测试验证通过**

Run: `node tests/userProfileRestingHr.test.js`
Expected: 6/6 passed

- [ ] **Step 5: Commit**

```bash
git add stores/userProfile.js tests/userProfileRestingHr.test.js
git commit -m "feat(profile): add restingHR field to userProfile store with 30-120 bpm validation"
```

---

## Task 3: utils/strengthTrainingAnalyzer.js 核心算法（TDD，分工具/窗口/积分/主流程 4 子轮）

本 Task 分 4 轮，每轮"写失败测试 → 实现 → 通过"。

**Files:**
- Create: `utils/strengthTrainingAnalyzer.js`
- Test: `tests/strengthTrainingAnalyzer.test.js`

### 轮 1：工具函数（calcVolumeLoad / estimateE1RMEpley / calcRelativeIntensity / calcHRR）

- [ ] **Step 1.1: 写失败测试（轮 1 test code，先放到 test 文件头部）**

```javascript
// tests/strengthTrainingAnalyzer.test.js  —— 追加至文件
import assert from 'assert'
import {
  calcVolumeLoad,
  estimateE1RMEpley,
  calcRelativeIntensity,
  calcHRR,
  extractHrWindow,
  calcHrMetrics,
  calcCardiovascularLoad,
  estimateRestingHr,
  analyzeTrainingSession,
  aggregateActionHrProfiles,
} from '../utils/strengthTrainingAnalyzer.js'

let passed = 0, failed = 0
function test(name, fn) {
  try { fn(); console.log(`✓ ${name}`); passed++ }
  catch (e) { console.log(`✗ ${name}\n  ${e.message}`); failed++ }
}

// 轮 1 tests
test('calcVolumeLoad: 50×10 = 500', () => assert.strictEqual(calcVolumeLoad(50, 10), 500))
test('estimateE1RMEpley: 100kg × 5 reps → 100 * (1+5/30) ≈ 116.67', () => {
  const r = estimateE1RMEpley(100, 5)
  assert.ok(Math.abs(r - 116.666) < 0.1, `expect 116.67, got ${r}`)
})
test('estimateE1RMEpley: bwMode 0 weight × 20 → 0', () => assert.strictEqual(estimateE1RMEpley(0, 20), 0))
test('calcRelativeIntensity: 80 / 116.67 ≈ 0.6857', () => {
  const r = calcRelativeIntensity(80, estimateE1RMEpley(100, 5))
  assert.ok(Math.abs(r - 0.6857) < 0.01, `expect 0.686, got ${r}`)
})
test('calcRelativeIntensity: e1RM=0 → null（不抛异常/除 0）', () => {
  assert.strictEqual(calcRelativeIntensity(50, 0), null)
  assert.strictEqual(calcRelativeIntensity(50, null), null)
})
test('calcHRR: (140-65)/(192-65)=75/127≈0.591', () => {
  assert.ok(Math.abs(calcHRR(140, 65, 192) - 0.5905) < 0.01)
})
test('calcHRR: clamp 低于 0 和高于 1', () => {
  assert.strictEqual(calcHRR(50, 65, 192), 0, 'HR < rHR 钳制 0')
  assert.strictEqual(calcHRR(200, 65, 192), 1, 'HR > mHR 钳制 1')
})
```

- [ ] **Step 1.2: 跑测试验证失败**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: FAIL（模块未创建）

- [ ] **Step 1.3: 实现 utils/strengthTrainingAnalyzer.js 顶部 4 个工具函数**

```javascript
// utils/strengthTrainingAnalyzer.js
/**
 * 力量训练心率分析纯算法模块（无副作用，不依赖 uni / Pinia / DOM）。
 * 所有参数传值式调用，便于 node 下单测。
 */

export function calcVolumeLoad(weight, reps) {
  const w = Number(weight) || 0
  const r = Number(reps) || 0
  return w * r
}

export function estimateE1RMEpley(weight, reps) {
  const w = Number(weight) || 0
  const r = Number(reps) || 0
  if (w <= 0 || r <= 0) return 0
  return w * (1 + r / 30)
}

export function calcRelativeIntensity(weight, oneRM) {
  const w = Number(weight) || 0
  const o = Number(oneRM) || 0
  if (!o || o <= 0) return null
  return w / o
}

export function calcHRR(hr, restingHR, maxHR) {
  const h = Number(hr) || 0
  const r = Number(restingHR) || 0
  const m = Number(maxHR) || 0
  if (m <= r) return 0 // 异常：退化保护
  const raw = (h - r) / (m - r)
  if (raw < 0) return 0
  if (raw > 1) return 1
  return raw
}
```

- [ ] **Step 1.4: 跑测试验证通过（轮 1 7 个用例）**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: 7/7 passed

---

### 轮 2：extractHrWindow + calcHrMetrics（含边界：采样不足 → analyzed=false 全 null）

- [ ] **Step 2.1: 追加失败测试（在 test 末尾添加后直接跑）**

```javascript
// 追加到同一 test 文件
// 制造 20 个样本：ts 0..19000 step 1000，hr 70..89
const samples = Array.from({ length: 20 }, (_, i) => ({ ts: i * 1000, hr: 70 + i }))

test('extractHrWindow 区间 [5s, 14s] → 10 个样本，analyzed=true', () => {
  const w = extractHrWindow(samples, 5000, 14000, 3)
  assert.strictEqual(w.sampleCount, 10)
  assert.strictEqual(w.analyzed, true)
  assert.deepStrictEqual(w.samples[0], { ts: 5000, hr: 75 })
})

test('extractHrWindow 区间只有 2 个样本（< minSamples=3）→ analyzed=false', () => {
  const w = extractHrWindow(samples, 0, 1500, 3)
  assert.strictEqual(w.analyzed, false)
  assert.strictEqual(w.sampleCount, 2)
})

test('extractHrWindow 区间完全不相交 → analyzed=false, sampleCount=0', () => {
  const w = extractHrWindow(samples, 99999, 999999, 3)
  assert.strictEqual(w.sampleCount, 0)
  assert.strictEqual(w.analyzed, false)
})

test('calcHrMetrics 正常 case: 10 samples(hr 75-84) → avg 79.5, peak 84, start=75, end=84, delta=9', () => {
  const w = extractHrWindow(samples, 5000, 14000, 3)
  const m = calcHrMetrics(w)
  assert.strictEqual(m.analyzed, true)
  assert.strictEqual(m.startHr, 75)
  assert.strictEqual(m.endHr, 84)
  assert.strictEqual(m.peakHr, 84)
  assert.strictEqual(Math.round(m.averageHr), 80, `avg=${m.averageHr}`)
  assert.strictEqual(m.deltaHr, 9) // peak - start
  assert.strictEqual(m.sampleCount, 10)
})

test('calcHrMetrics analyzed=false → 所有数值字段严格为 null（不填 0 等假值）', () => {
  const w = extractHrWindow(samples, 0, 1500, 3)
  const m = calcHrMetrics(w)
  assert.strictEqual(m.analyzed, false)
  assert.strictEqual(m.startHr, null)
  assert.strictEqual(m.averageHr, null)
  assert.strictEqual(m.peakHr, null)
  assert.strictEqual(m.endHr, null)
  assert.strictEqual(m.deltaHr, null)
  assert.strictEqual(m.sampleCount, 2) // count 本身不是 HR 字段，保留真实数
})
```

- [ ] **Step 2.2: 跑测试验证失败**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: FAIL（`extractHrWindow`/`calcHrMetrics` 未导出）

- [ ] **Step 2.3: 实现 extractHrWindow + calcHrMetrics**

```javascript
// utils/strengthTrainingAnalyzer.js 追加

export function extractHrWindow(hrSamplesWithTs, startTime, endTime, minSamples = 3) {
  const arr = Array.isArray(hrSamplesWithTs) ? hrSamplesWithTs : []
  const st = Number(startTime) || 0
  const et = Number(endTime) || 0
  const samples = []
  for (const s of arr) {
    if (!s || typeof s.ts !== 'number' || typeof s.hr !== 'number') continue
    if (s.ts >= st && s.ts <= et) samples.push(s)
  }
  return {
    samples,
    sampleCount: samples.length,
    analyzed: samples.length >= minSamples,
  }
}

export function calcHrMetrics(window) {
  if (!window || !window.analyzed) {
    return {
      analyzed: false,
      startHr: null,
      averageHr: null,
      peakHr: null,
      endHr: null,
      deltaHr: null,
      sampleCount: window ? (window.sampleCount | 0) : 0,
    }
  }
  const { samples } = window
  let sum = 0
  let peak = -Infinity
  for (const s of samples) {
    sum += s.hr
    if (s.hr > peak) peak = s.hr
  }
  const start = samples[0].hr
  const end = samples[samples.length - 1].hr
  const avg = sum / samples.length
  return {
    analyzed: true,
    startHr: start,
    averageHr: Math.round(avg * 10) / 10,
    peakHr: peak,
    endHr: end,
    deltaHr: peak - start,
    sampleCount: samples.length,
  }
}
```

- [ ] **Step 2.4: 跑测试验证通过**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: 7+5=12/12 passed

---

### 轮 3：calcCardiovascularLoad（HRR 梯形积分） + estimateRestingHr（三分支）+ age=0 兼容

- [ ] **Step 3.1: 追加失败测试**

```javascript
// 追加
test('calcCardiovascularLoad: 2 samples (0s HR70, 60s HR130) rHR=65 mHR=192 → 手工核对≈0.308 HRR·min', () => {
  // Δt=1min, HRR0=5/127=0.039, HRR1=65/127=0.5118 → avg=0.2754 → 积分 0.2754
  const r = calcCardiovascularLoad([{ ts: 0, hr: 70 }, { ts: 60000, hr: 130 }], 65, 192, 0, 60000)
  assert.ok(Math.abs(r - 0.275) < 0.01, `got ${r}`)
})
test('calcCardiovascularLoad: 单点 → null', () => {
  assert.strictEqual(calcCardiovascularLoad([{ ts: 0, hr: 100 }], 65, 192, 0, 0), null)
})
test('calcCardiovascularLoad: restingHR >= maxHR 保护 → null 不崩', () => {
  assert.strictEqual(calcCardiovascularLoad([{ ts: 0, hr: 100 }, { ts: 60000, hr: 120 }], 200, 192, 0, 60000), null)
})

// estimateRestingHr
const earlySamples = Array.from({ length: 30 }, (_, i) => ({
  ts: i * 1000,
  hr: 65 + ((i + 5) % 10) // 65-74 周期，60s 窗口内最低是 65
}))
test('estimateRestingHr: 60s 内 30 samples → source=window, value=65', () => {
  const r = estimateRestingHr(earlySamples, 0, 60, 30)
  assert.strictEqual(r.source, 'window')
  assert.strictEqual(r.value, 65)
})
const fewSamples = [{ ts: 0, hr: 80 }, { ts: 1000, hr: 82 }]
test('estimateRestingHr: 2 samples + age 30(mHR=190) → source=formula, value=round(190*0.4)=76', () => {
  const r = estimateRestingHr(fewSamples, 0, 60, 30)
  assert.strictEqual(r.source, 'formula')
  assert.strictEqual(r.value, 76)
})
test('estimateRestingHr: 2 samples + age 0/null fallback-age 跳过 → source=none, value=null', () => {
  const r = estimateRestingHr(fewSamples, 0, 60, null)
  assert.strictEqual(r.source, 'none')
  assert.strictEqual(r.value, null)
})
```

- [ ] **Step 3.2: 跑测试验证失败**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: FAIL（两函数未导出）

- [ ] **Step 3.3: 实现两函数**

```javascript
// utils/strengthTrainingAnalyzer.js 追加

export function calcCardiovascularLoad(hrSamplesWithTs, restingHR, maxHR, sessionStartTs, sessionEndTs) {
  const arr = Array.isArray(hrSamplesWithTs) ? hrSamplesWithTs : []
  const r = Number(restingHR) || 0
  const m = Number(maxHR) || 0
  const st = Number(sessionStartTs) || 0
  const et = Number(sessionEndTs) || Date.now()
  if (r >= m) {
    console.warn('[cardiovascularLoad] restingHR >= maxHR, skipped → null')
    return null
  }
  // 取 session 范围内样本（可能未排序，先按 ts 过滤再排序）
  const inRange = []
  for (const s of arr) {
    if (!s || typeof s.ts !== 'number' || typeof s.hr !== 'number') continue
    if (s.ts >= st && s.ts <= et) inRange.push(s)
  }
  if (inRange.length < 2) return null
  inRange.sort((a, b) => a.ts - b.ts)
  let total = 0
  for (let i = 1; i < inRange.length; i++) {
    const dtMin = (inRange[i].ts - inRange[i - 1].ts) / 60000
    if (dtMin <= 0) continue
    const avgHRR = (calcHRR(inRange[i - 1].hr, r, m) + calcHRR(inRange[i].hr, r, m)) / 2
    total += dtMin * avgHRR
  }
  return Math.round(total * 100) / 100
}

export function estimateRestingHr(hrSamplesWithTs, sessionStartTs, windowSec = 60, fallbackAge = null) {
  const st = Number(sessionStartTs) || 0
  const end = st + (windowSec * 1000)
  const win = []
  const arr = Array.isArray(hrSamplesWithTs) ? hrSamplesWithTs : []
  for (const s of arr) {
    if (!s || typeof s.ts !== 'number' || typeof s.hr !== 'number') continue
    if (s.ts >= st && s.ts <= end) win.push(s.hr)
  }
  if (win.length >= 10) {
    return { value: Math.min(...win), source: 'window' }
  }
  // fallback 公式
  const age = Number(fallbackAge)
  if (age && age > 0) {
    const maxHR = 220 - age
    const v = Math.round(maxHR * 0.4)
    return { value: v, source: 'formula' }
  }
  return { value: null, source: 'none' }
}
```

- [ ] **Step 3.4: 跑测试验证通过**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: 12+7=19/19 passed

---

### 轮 4：analyzeTrainingSession（主流程） + aggregateActionHrProfiles（长期聚合）

- [ ] **Step 4.1: 追加失败测试**

```javascript
// 构造 3 组 FlatEntry：2 组卧推（timestamp 1000/190000 transition 189s），1 组下拉（timestamp=200000）—— 第 2 组 transition=11s，rest=180，estimatedSetDur=0 → HR analyzed=false
const now0 = 1700000000000
const entries = [
  { actionName: '杠铃卧推', setIndex: 1, timestamp: now0 + 1000, weight: 60, reps: 10, volumeLoad: 600 },
  { actionName: '杠铃卧推', setIndex: 2, timestamp: now0 + 190000, weight: 60, reps: 10, volumeLoad: 600 },
  { actionName: '高位下拉', setIndex: 1, timestamp: now0 + 201000, weight: 50, reps: 12, volumeLoad: 600 },
]
// HR 样本：210s 跨度，每秒 1 个，hr = 75 + 每秒线性上升到 now0+200s 时 150
const hrCount = 210
const hrSamples = Array.from({ length: hrCount }, (_, i) => ({
  ts: now0 + i * 1000,
  hr: Math.round(75 + i * 75 / hrCount), // 75 → 150 线性
}))
const restFn = () => 180 // 默认 heavy 180s
const profile = { gender: 'male', age: 30, weight: 70, restingHR: 65 } // age 30 → maxHR 190
const histBest = (name) => (name === '杠铃卧推' ? { bestWeight: 100, bestReps: 5 } : null) // e1RM ≈ 116.67
const result = analyzeTrainingSession({
  entries,
  hrSamplesWithTs: hrSamples,
  restDurationSecByAction: restFn,
  profile,
  historicalBestGetter: histBest,
})

// —— 断言开始 ——
test('analyzeTrainingSession: session.totalVolumeLoad = 600+600+600 = 1800', () => {
  assert.strictEqual(result.session.totalVolumeLoad, 1800)
  assert.strictEqual(result.session.mechanicalLoad, 1800)
})
test('analyzeTrainingSession: cardiovascularLoad 非 null（HR 够）', () => {
  assert.ok(typeof result.session.cardiovascularLoad === 'number' && result.session.cardiovascularLoad > 0)
})
test('analyzeTrainingSession: restingHRUsed 来自 profile(65)，maxHR=190 formula', () => {
  assert.deepStrictEqual(result.session.restingHrUsed, { value: 65, source: 'profile' })
  assert.deepStrictEqual(result.session.maxHrUsed, { value: 190, source: 'formula' })
})
test('analyzeTrainingSession: setAnalyses 长度=3，按时间升序', () => {
  assert.strictEqual(result.setAnalyses.length, 3)
  assert.strictEqual(result.setAnalyses[0].setIndex, 1)
})
test('analyzeTrainingSession: 第 2 组 transition=11s, rest=180s → estimatedSetDuration=0 → hr.analyzed=false', () => {
  const s = result.setAnalyses[1]
  assert.strictEqual(s.transitionTimeSec, 11, `expect 11 got ${s.transitionTimeSec}`)
  assert.strictEqual(s.estimatedSetDurationSec, 0)
  assert.strictEqual(s.hr.analyzed, false)
})
test('analyzeTrainingSession: 卧推 set1 relativeIntensity = 60 / 116.67 ≈ 0.514，e1RMEstimated=true', () => {
  const r = result.setAnalyses[0].relativeIntensity
  assert.strictEqual(r.e1RMEstimated, true)
  assert.strictEqual(r.source, 'epley')
  assert.ok(Math.abs(r.value - 0.514) < 0.01, `got ${r.value}`)
})
test('analyzeTrainingSession: 高位下拉 无历史最佳 → relativeIntensity=null', () => {
  const r = result.setAnalyses[2].relativeIntensity
  assert.strictEqual(r.value, null)
  assert.strictEqual(r.e1RM, null)
})
test('analyzeTrainingSession: actionAnalyses 有 2 动作，卧推 setCount=2，下拉=1', () => {
  assert.strictEqual(result.actionAnalyses['杠铃卧推'].setCount, 2)
  assert.strictEqual(result.actionAnalyses['高位下拉'].setCount, 1)
  assert.ok(result.actionAnalyses['杠铃卧推'].analyzedSetCount <= 2)
})
test('analyzeTrainingSession: entries empty → 返回安全值(0/null)，不崩', () => {
  const emp = analyzeTrainingSession({ entries: [], hrSamplesWithTs: [], restDurationSecByAction: () => 0, profile: { ...profile }, historicalBestGetter: () => null })
  assert.strictEqual(emp.session.totalVolumeLoad, 0)
  assert.strictEqual(emp.setAnalyses.length, 0)
  assert.strictEqual(emp.status, 'in_progress')
})

// age=0 保护测试
const age0Result = analyzeTrainingSession({
  entries, hrSamplesWithTs: hrSamples,
  restDurationSecByAction: restFn,
  profile: { ...profile, age: 0 },
  historicalBestGetter: histBest,
})
test('analyzeTrainingSession: profile.age=0 → maxHRUsed source=age-missing value=null, cardiovascularLoad=null, set级hr照常analyzed', () => {
  assert.deepStrictEqual(age0Result.session.maxHrUsed, { value: null, source: 'age-missing' })
  assert.strictEqual(age0Result.session.cardiovascularLoad, null)
  assert.ok(age0Result.setAnalyses[0].hr.analyzed, `set-level hr unaffected, analyzed=${age0Result.setAnalyses[0].hr.analyzed}`)
})

// aggregateActionHrProfiles 简易验证：传入 2 日 analysis
const day1 = { date: '2026-08-29', analysis: result }
// 造 day2：卧推 80kg ×8（更重 → 另一个桶）
const entries2 = [
  { actionName: '杠铃卧推', setIndex: 1, timestamp: now0 + 86400000 + 1000, weight: 80, reps: 8, volumeLoad: 640 },
]
const hr2 = [{ ts: now0 + 86400000 + 1000, hr: 100 }, { ts: now0 + 86400000 + 2000, hr: 140 }, { ts: now0 + 86400000 + 3000, hr: 145 }, { ts: now0 + 86400000 + 4000, hr: 138 }, { ts: now0 + 86400000 + 5000, hr: 130 }]
const r2 = analyzeTrainingSession({ entries: entries2, hrSamplesWithTs: hr2, restDurationSecByAction: restFn, profile, historicalBestGetter: histBest })
const day2 = { date: '2026-08-30', analysis: r2 }
const profiles = aggregateActionHrProfiles([day1, day2], '杠铃卧推')

test('aggregateActionHrProfiles: 卧推 2 日 → 至少存在 2 个非空桶（60 & 80kg），每桶 sampleSets >=1', () => {
  const nonEmpty = profiles.filter(p => p.sampleSets >= 1)
  assert.ok(nonEmpty.length >= 2, `nonEmpty buckets=${nonEmpty.length} expect >=2, total=${profiles.length}`)
  const bk60 = profiles.find(p => p.weightBucketLower === 60)
  const bk80 = profiles.find(p => p.weightBucketLower === 80)
  assert.ok(bk60 && bk60.sampleSets >= 1, '60kg bucket 有样本')
  assert.ok(bk80 && bk80.sampleSets >= 1, '80kg bucket 有样本')
  assert.ok(Array.isArray(bk60.points), '散点 points 为数组')
})
```

- [ ] **Step 4.2: 跑测试验证失败**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: FAIL（analyzeTrainingSession / aggregateActionHrProfiles 未实现）

- [ ] **Step 4.3: 实现 analyzeTrainingSession + aggregateActionHrProfiles**

```javascript
// utils/strengthTrainingAnalyzer.js 追加

function numOr(v, d = 0) { const n = Number(v); return Number.isNaN(n) ? d : n }

export function analyzeTrainingSession(params) {
  const { entries = [], hrSamplesWithTs = [], restDurationSecByAction, profile, historicalBestGetter } = params || {}

  const out = {
    generatedAt: Date.now(),
    sessionStartTs: 0,
    sessionEndTs: null,
    status: 'in_progress',
    session: {
      totalVolumeLoad: 0,
      mechanicalLoad: 0,
      cardiovascularLoad: null,
      averageHr: null,
      peakHr: null,
      restingHrUsed: { value: null, source: 'estimated' },
      maxHrUsed: { value: null, source: 'formula' },
    },
    setAnalyses: [],
    actionAnalyses: {},
    hrProfiles: {},
  }

  if (!entries.length) return out

  // Step 2 - session 时间范围
  const tsList = entries.map(e => e.timestamp).filter(t => typeof t === 'number' && t > 0)
  out.sessionStartTs = tsList.length ? Math.min(...tsList) : Date.now()
  const maxTs = tsList.length ? Math.max(...tsList) : 0
  out.sessionEndTs = maxTs > 0 ? maxTs : Date.now()

  // Step 3 - restingHR / maxHR 决策
  const age = numOr(profile && profile.age, 0)
  let maxHR = null
  if (age > 0) {
    maxHR = 220 - age
    out.session.maxHrUsed = { value: maxHR, source: 'formula' }
  } else {
    out.session.maxHrUsed = { value: null, source: 'age-missing' }
  }
  let restingHR = null
  if (profile && profile.restingHR && profile.restingHR >= 30) {
    restingHR = profile.restingHR
    out.session.restingHrUsed = { value: restingHR, source: 'profile' }
  } else {
    const est = estimateRestingHr(hrSamplesWithTs, out.sessionStartTs, 60, age > 0 ? age : null)
    if (est.value !== null) restingHR = est.value
    out.session.restingHrUsed = { value: restingHR, source: 'estimated' } // 若仍 null 则 value:null estimated
  }

  // Step 4 - 每组分析
  for (let i = 0; i < entries.length; i++) {
    const e = entries[i]
    const next = i + 1 < entries.length ? entries[i + 1] : null
    const transitionTimeSec = (next && e.timestamp && next.timestamp)
      ? Math.max(0, Math.round((next.timestamp - e.timestamp) / 1000))
      : null
    const restDur = numOr(restDurationSecByAction ? restDurationSecByAction(e.actionName) : 0, 0)
    const estimatedSetDurationSec = (transitionTimeSec === null) ? null : Math.max(0, transitionTimeSec - restDur)
    // transition 与 estimated duration 在 data contract 上按 spec 存储

    // 机械
    const w = numOr(e.weight, 0)
    const r = numOr(e.reps, 0)
    const vl = w * r
    // Epley e1RM
    let e1RM = null
    let riSource = null
    let riValue = null
    const best = historicalBestGetter ? historicalBestGetter(e.actionName) : null
    if (best && best.bestWeight > 0 && best.bestReps > 0) {
      e1RM = estimateE1RMEpley(best.bestWeight, best.bestReps)
      if (e1RM > 0) {
        riValue = calcRelativeIntensity(w, e1RM)
        riSource = 'epley'
      }
    }

    // HR 窗口
    let winResult
    if (typeof e.timestamp === 'number' && e.timestamp > 0) {
      const startWin = e.timestamp
      let endWin
      if (estimatedSetDurationSec !== null && estimatedSetDurationSec > 0) {
        endWin = startWin + estimatedSetDurationSec * 1000
      } else if (next && next.timestamp) {
        endWin = next.timestamp
      } else {
        endWin = out.sessionEndTs || Date.now()
      }
      winResult = extractHrWindow(hrSamplesWithTs, startWin, endWin, 3)
    } else {
      winResult = { samples: [], sampleCount: 0, analyzed: false }
    }
    const metrics = calcHrMetrics(winResult)

    out.setAnalyses.push({
      actionName: e.actionName,
      setIndex: numOr(e.setIndex, 1),
      timestamp: e.timestamp,
      weight: w,
      reps: r,
      volumeLoad: vl,
      relativeIntensity: { value: riValue, e1RM, e1RMEstimated: !!riSource, source: riSource },
      transitionTimeSec,
      restDurationSec: restDur,
      estimatedSetDurationSec,
      hr: metrics,
    })
  }

  // Step 5 - session 汇总
  out.session.totalVolumeLoad = out.setAnalyses.reduce((s, a) => s + a.volumeLoad, 0)
  out.session.mechanicalLoad = out.session.totalVolumeLoad
  if (maxHR !== null && restingHR !== null && restingHR < maxHR) {
    out.session.cardiovascularLoad = calcCardiovascularLoad(hrSamplesWithTs, restingHR, maxHR, out.sessionStartTs, out.sessionEndTs)
  }
  // session-wide HR avg/peak（不管 set 级窗口，直接取 session 范围全部采样，更稳定）
  const sessWin = extractHrWindow(hrSamplesWithTs, out.sessionStartTs, out.sessionEndTs || Date.now(), 1)
  if (sessWin.sampleCount > 0) {
    const hrs = sessWin.samples.map(s => s.hr)
    out.session.averageHr = Math.round(hrs.reduce((s, h) => s + h, 0) / hrs.length * 10) / 10
    out.session.peakHr = Math.max(...hrs)
  }

  // Step 6 - actionAnalyses
  for (const set of out.setAnalyses) {
    const k = set.actionName
    if (!out.actionAnalyses[k]) {
      out.actionAnalyses[k] = { actionName: k, setCount: 0, totalVolume: 0, averageHr: null, peakHr: null, avgDeltaHr: null, analyzedSetCount: 0 }
    }
    const aa = out.actionAnalyses[k]
    aa.setCount += 1
    aa.totalVolume += set.volumeLoad
    if (set.hr.analyzed) {
      aa.analyzedSetCount += 1
      // 运行均值（只存结果时用数组临时累加，最后除）
      aa._sumAvg = (aa._sumAvg || 0) + set.hr.averageHr
      aa._maxPeak = aa._maxPeak === undefined ? set.hr.peakHr : Math.max(aa._maxPeak, set.hr.peakHr)
      aa._sumDelta = (aa._sumDelta || 0) + set.hr.deltaHr
    }
  }
  // 收尾：把临时 _sum → average/peak/avgDeltaHr
  for (const k of Object.keys(out.actionAnalyses)) {
    const aa = out.actionAnalyses[k]
    if (aa.analyzedSetCount > 0) {
      aa.averageHr = Math.round(aa._sumAvg / aa.analyzedSetCount * 10) / 10
      aa.peakHr = aa._maxPeak
      aa.avgDeltaHr = Math.round(aa._sumDelta / aa.analyzedSetCount * 10) / 10
    }
    delete aa._sumAvg; delete aa._maxPeak; delete aa._sumDelta
  }

  return out
}

const WEIGHT_BUCKETS = [20, 30, 40, 50, 60, 70, 80, 100, 120]
function bucketFor(w) {
  for (let i = WEIGHT_BUCKETS.length - 1; i >= 0; i--) {
    if (w >= WEIGHT_BUCKETS[i]) {
      return { lower: WEIGHT_BUCKETS[i], label: i === WEIGHT_BUCKETS.length - 1 ? '120+' : String(WEIGHT_BUCKETS[i]) }
    }
  }
  return { lower: 0, label: '<20' }
}

export function aggregateActionHrProfiles(historicalAnalyses, targetActionName) {
  // 初始化所有桶
  const buckets = {}
  for (const b of WEIGHT_BUCKETS) buckets[b] = { weightBucket: String(b), weightBucketLower: b, samples: [], points: [] }
  buckets['120+'] = { weightBucket: '120+', weightBucketLower: 120, samples: [], points: [] }
  if (!buckets['<20']) buckets['<20'] = { weightBucket: '<20', weightBucketLower: 0, samples: [], points: [] }

  const arr = Array.isArray(historicalAnalyses) ? historicalAnalyses : []
  for (const hist of arr) {
    const analysis = hist && hist.analysis
    if (!analysis || !Array.isArray(analysis.setAnalyses)) continue
    for (const set of analysis.setAnalyses) {
      if (set.actionName !== targetActionName) continue
      if (!set.hr.analyzed) continue
      const { label, lower } = bucketFor(set.weight)
      const key = label
      if (!buckets[key]) buckets[key] = { weightBucket: key, weightBucketLower: lower, samples: [], points: [] }
      buckets[key].samples.push({ avgHr: set.hr.averageHr, peakHr: set.hr.peakHr, deltaHr: set.hr.deltaHr })
      buckets[key].points.push({ date: hist.date, weight: set.weight, reps: set.reps, avgHr: set.hr.averageHr, peakHr: set.hr.peakHr, deltaHr: set.hr.deltaHr })
    }
  }
  // 整理结果数组
  const keysOrder = ['<20>', ...WEIGHT_BUCKETS.map(b => String(b)), '120+']
  return keysOrder
    .filter(k => buckets[k])
    .map(k => {
      const b = buckets[k]
      const n = b.samples.length
      if (n === 0) {
        return { weightBucket: b.weightBucket, weightBucketLower: b.weightBucketLower, avgHr: null, peakHr: null, avgDeltaHr: null, sampleSets: 0, points: [] }
      }
      let sAvg = 0, sPeak = 0, sDelta = 0
      for (const s of b.samples) {
        sAvg += s.avgHr; sPeak += s.peakHr; sDelta += s.deltaHr
      }
      return {
        weightBucket: b.weightBucket,
        weightBucketLower: b.weightBucketLower,
        avgHr: Math.round(sAvg / n * 10) / 10,
        peakHr: Math.round(sPeak / n * 10) / 10,
        avgDeltaHr: Math.round(sDelta / n * 10) / 10,
        sampleSets: n,
        points: b.points,
      }
    })
}
```

- [ ] **Step 4.4: 跑测试验证通过**

Run: `node tests/strengthTrainingAnalyzer.test.js`
Expected: 19 + 12 = 31/31 passed（含工具/窗口/积分/主流程 共 31 个断言）

- [ ] **Step 4.5: Commit 整个 algorithm 模块**

```bash
git add utils/strengthTrainingAnalyzer.js tests/strengthTrainingAnalyzer.test.js
git commit -m "feat(analysis): add strengthTrainingAnalyzer pure functions + 31 test cases (Epley/HRR/Window/CardioLoad/Session/Profile)"
```

---

## Task 4: calorieEstimate.js + metEstimate.js 加 deprecate 注释（不删文件，向后兼容回滚）

**Files:**
- Modify: `utils/calorieEstimate.js`（文件顶部 + 每个导出函数 JSDoc 加 deprecate）
- Modify: `utils/metEstimate.js`（同上）
- No new test（仅注释，不改变运行时行为）

- [ ] **Step 1: calorieEstimate.js 文件顶部加注释块**

```javascript
/**
 * @deprecated 2026-08-30 力量训练心率分析模块上线后，训练页运行时不再进行热量估算。
 *  本文件保留用于旧 dayData 恢复（历史 caloriesTotal/durationSec 只读），以及未来若需恢复"热量"展示时回滚参考。
 *  替代方案：strengthTrainingAnalyzer.js（Mechanical / Cardiovascular 双指标分离展示，不再合成 kcal）。
 */
```

在 `export function estimateAvgHr` / `export function calcCaloriesByKeytel` 前分别加 `/** @deprecated 2026-08-30 已废弃，不再被 day.vue 新流程调用。*/`。

- [ ] **Step 2: metEstimate.js 顶部同风格 deprecated**

```javascript
/**
 * @deprecated 2026-08-30 力量训练心率分析模块上线后，训练页运行时不再使用 MET 方法估算热量。
 *  文件保留用于历史 dayData 读取（metValue / metActivityKey 字段兼容）。
 *  替代方案：strengthTrainingAnalyzer.js。
 */
```

`export function calcMetCalories` 前同样加 `@deprecated`。

- [ ] **Step 3: 手工验证（打开 day.vue 不会因 deprecate 注释出语法问题）**

- [ ] **Step 4: Commit**

```bash
git add utils/calorieEstimate.js utils/metEstimate.js
git commit -m "docs(deprecate): mark calorieEstimate.js and metEstimate.js as deprecated 2026-08-30 (replaced by strengthTrainingAnalyzer)"
```

---

## Task 5: HeartRateToggle.vue —— 移除 kcal/duration 展开态行（保留 props 防止父组件报错）

**Files:**
- Modify: `components/HeartRateToggle.vue`（template `.hr-expanded .hr-kcal-blk` 整行删除 / props 保留 deprecated 注释 / 样式宽度调整）

**前期确认（读代码）**：先 Read HeartRateToggle.vue 找到模板中 `kcalTotal` 引用的整段 DOM 以及 `kcalTotal`/`durationSec` props 声明位置。

- [ ] **Step 1: 删除展开态 kcal block（整段，含 "🔥xxx kcal / mm:ss"）**

在 template 中找到 `.hr-expanded` 容器里 `.hr-kcal-blk`（或语义同：包含 `{{ kcalTotal }}` + 时长显示）的整个 `<view class="hr-kcal-blk">...</view>` 块，**完整删除**。保留：心率值行、区间条/档位、指导语（剩下三列）。

删除后展开态布局从 4 行 → 3 行。为避免高度塌陷，`.hr-expanded` 原 padding/height 不动；剩下三列在 `style` 中将 `justify-content: space-between` 调整为三列平均分布（`justify-content: space-around` 或 `display:grid grid-template-columns: 1fr 1fr 1fr`，按项目既有风格）。

- [ ] **Step 2: props kcalTotal / durationSec 保留，加 // @deprecated 注释**

在 `props:` 中找到：
```javascript
kcalTotal: { type: Number, default: 0 },
durationSec: { type: Number, default: 0 },
```
每一项前加注释：
```javascript
/** @deprecated 2026-08-30 训练分析模块已废止热量/时长累计，prop 保留仅为避免父组件传参报错，不再参与 UI 渲染。 */
```

- [ ] **Step 3: 样式清理**

如果有 `.hr-kcal-blk {}` 专用样式（字体颜色、宽度等），**保留不删除**（只是不再被 DOM 使用；避免 CSS 报错与回滚简单）。如果样式里设置了 `.hr-expanded` 的 min-height 依赖四行，按 3 行视觉微调（约减少 36px 行高）。

- [ ] **Step 4: 手工验证（微信开发者工具）**

打开 day.vue → 心率按钮展开态：
- Expected: 心率值 ♥135 + 区间条 + 档位 + 指导语 → 三列水平排布紧凑，无空位塌陷。
- Expected: 不再出现 "🔥xxx kcal / 0:xx" 任何文字。
- Expected: day.vue 控制台无 prop 告警（即使 day.vue 此时仍传 kcalTotal/durationSec 过来，props 存在所以不报错）。

- [ ] **Step 5: Commit**

```bash
git add components/HeartRateToggle.vue
git commit -m "ui(toggle): remove kcal+duration row from expanded HeartRateToggle (props preserved for backward compat)"
```

---

## Task 6: HeartRateChartPopup.vue —— 移除热量 stat + "时长"改为"采样数"

**Files:**
- Modify: `components/HeartRateChartPopup.vue`（`.chart-footer` 5 格 → 4 格）

- [ ] **Step 1: 删除 footer 中 "热量" stat 整格**

Read 确认 5 格布局结构：`<view class="chart-stat"><div class="stat-label">热量</div>...` 这一格，**完整删除**。

- [ ] **Step 2: 将"时长" stat 改为"采样数"，显示值改为 samples 长度（秒数语义替代）**

找到 `<div class="stat-label">时长</div>` 这一格，改为：
```html
<view class="chart-stat">
  <view class="stat-label">采样数</view>
  <view class="stat-value">{{ (hrSamplesWithTs && hrSamplesWithTs.length) || hrSamples.length }}</view>
</view>
```
在 props 里也加 `hrSamplesWithTs: { type: Array, default: () => [] }` 作为新可选 prop（优先用带 ts 的长度，缺省 fallback 旧 hrSamples 长度）。

- [ ] **Step 3: 样式调整**

原 5 列 → 改后 4 列。CSS `.chart-footer` 的 `display:grid` 若 `grid-template-columns: repeat(5,1fr)`，改为 `repeat(4,1fr)`；若是 flex 布局则无需改。

- [ ] **Step 4: 手工验证**

day.vue → 点心率按钮 → 打开图表弹窗：
- Expected: footer 4 格：平均 / 最高 / 最低 / 采样数
- Expected: "采样数"数字 ≈ 当前 BLE 连接持续秒数

- [ ] **Step 5: Commit**

```bash
git add components/HeartRateChartPopup.vue
git commit -m "ui(chart-popup): remove calories stat, rename duration→sample count (sourced from hrSamplesWithTs.length)"
```

---

## Task 7: BodyProfilePopup.vue —— 新增静息心率 restingHR 数字输入（与 Task 2 store 联动）

**Files:**
- Modify: `components/BodyProfilePopup.vue`（体重输入行下方插入 restingHR 行）

**前置 Read**：先 Read BodyProfilePopup.vue，找到体重（`体重 / bodyWeight`）输入行的 DOM 结构。需要精准找到它的包裹块，按同样结构复制一行。

- [ ] **Step 1: 插入 restingHR 输入 DOM（体重行后紧跟一行）**

```html
<view class="profile-row">
  <view class="profile-label">静息心率<text class="optional-tag">可选</text></view>
  <view class="profile-input-wrap">
    <input
      class="profile-input"
      type="number"
      v-model="localRestingHr"
      placeholder="如：65"
      :maxlength="3"
    />
    <text class="profile-unit">bpm</text>
  </view>
</view>
<view class="profile-hint" style="padding: 4px 16px 10px;">
  未填写时自动用训练初期 60 秒最低心率估算，结果标记为 estimated。（建议范围 30-120 bpm）
</view>
```

其中 `<text class="optional-tag">` 若项目现有通用标签 class，复用之；否则自写简单行内样式 `font-size:11px; color:var(--text-muted); margin-left:4px;`。

- [ ] **Step 2: script 加 localRestingHr 状态 + 初始化/保存联动**

data() 中：`localRestingHr: ''`（用空字符串而不是 number，避免清空输入框时显示 0）。

在 `mounted()` 或 profile 回填处（从 useUserProfile() store 读取当前值后赋值）：
```javascript
const profile = this.profileStore.toProfile()
this.localRestingHr = (profile.restingHR ?? '').toString()
```

在保存按钮 `onSave` 方法内，加：
```javascript
let restingHR = null
const trimmed = (this.localRestingHr ?? '').toString().trim()
if (trimmed !== '') {
  const n = Number(trimmed)
  if (!Number.isNaN(n) && n >= 30 && n <= 120) restingHR = n
  // 越界值：按 store setProfile 中的"拒绝 强制 null"策略直接不传，避免 UI 与 store 判定重复
  // 这里做一次与 store 一致的校验即可，用户输入 25/150 等会被静默转为 null（提示已在 profile-hint 行告知范围）
}
this.profileStore.setProfile({ ...otherFields, restingHR })
```

- [ ] **Step 3: 样式对齐**

身高/体重的输入行如果有 `border-bottom`，restingHR 行与之保持一致，避免多一条底纹或缺失。

- [ ] **Step 4: 手工验证**

打开 day.vue → 右上角 ⚙ → 身体档案：
- Expected: 体重行下方出现新的"静息心率（可选）"行 + 提示文字
- 验证①：输入 `65` → 保存 → 关闭弹窗 → 再打开 → 值仍为 65 ✓
- 验证②：清空 → 保存 → 再打开 → 值为空 ✓
- 验证③：输入 `150`（越界）→ 保存 → 再打开 → 值为空（静默拒绝，与 store 逻辑一致）✓
- 验证④：输入 `25` → 同上，静默拒绝 ✓

- [ ] **Step 5: Commit**

```bash
git add components/BodyProfilePopup.vue
git commit -m "ui(profile): add optional restingHR bpm input to BodyProfilePopup (sync with userProfile store restingHR field)"
```

---

## Task 8: pages.json 注册 trainingAnalysis 页面 + 目录骨架

**Files:**
- Create: `pages/trainingAnalysis/` 目录骨架（空文件先占位，Task 11 写代码）
- Modify: `pages.json`（追加新页面条目）

- [ ] **Step 1: 建目录骨架占位**

Run（PowerShell）：
```powershell
New-Item -ItemType Directory -Force -Path "d:\小程序\FitNote\pages\trainingAnalysis"
New-Item -ItemType Directory -Force -Path "d:\小程序\FitNote\pages\trainingAnalysis\components"
# 空占位文件，确保页面路由注册不报错（空 .vue 会解析失败 → 改为最小空 Vue 文件）
```

创建最小占位：
```powershell
# trainingAnalysis.vue (最小占位)
Set-Content -Path "d:\小程序\FitNote\pages\trainingAnalysis\trainingAnalysis.vue" -Value @"
<template>
  <view class="ta-empty">
    <text>Training Analysis Placeholder</text>
  </view>
</template>
<script> export default { name: 'training-analysis-page' } </script>
<style> .ta-empty { padding: 40px; text-align: center; color: gray; } </style>
"@
# HrBarChart.vue 最小占位
Set-Content -Path "d:\小程序\FitNote\pages\trainingAnalysis\components\HrBarChart.vue" -Value @"
<template><view class="hrbar-empty">HrBarChart placeholder</view></template>
<script> export default { name: 'hr-bar-chart', props: { data: Array } } </script>
<style> .hrbar-empty { padding: 24px; color: gray; } </style>
"@
# HrProfileChart.vue 最小占位
Set-Content -Path "d:\小程序\FitNote\pages\trainingAnalysis\components\HrProfileChart.vue" -Value @"
<template><view class="hrpf-empty">HrProfileChart placeholder</view></template>
<script> export default { name: 'hr-profile-chart', props: { profiles: Array } } </script>
<style> .hrpf-empty { padding: 24px; color: gray; } </style>
"@
```

- [ ] **Step 2: pages.json 追加页面条目（与现有条目风格一致）**

Read pages.json，找到 `pages: [` 数组的末尾（最后一个页面条目后）追加：
```json
{
  "path": "pages/trainingAnalysis/trainingAnalysis",
  "style": {
    "navigationBarTitleText": "训练分析",
    "navigationBarBackgroundColor": "#121212",
    "navigationBarTextStyle": "white"
  }
}
```
**注意**：JSON 末尾不得有逗号，需确认前一条目尾加逗号。

- [ ] **Step 3: 手工验证**

HBuilder / 微信开发者工具：
- Expected: uni-app 重新编译不报 `pages.json` 语法错误
- Expected: 直接 navigateTo `/pages/trainingAnalysis/trainingAnalysis`（可通过控制台命令测试）→ 能跳转到 Training Analysis Placeholder 空白页 ✓

- [ ] **Step 4: Commit**

```bash
git add pages.json pages/trainingAnalysis/
git commit -m "chore(routing): register pages/trainingAnalysis in pages.json + directory placeholders (to be filled in Task 11)"
```

---

## Task 9: TrainingAnalysisCard.vue（day.vue 底部折叠卡片）

**Files:**
- Create: `components/TrainingAnalysisCard.vue`
- Depends-on: Task 1 (dayHelper timestamp) / Task 2 (restingHR store) / Task 3 (analyzer 模块)

- [ ] **Step 1: 创建组件 script 壳（props + 折叠态）**

```javascript
// components/TrainingAnalysisCard.vue — script
import { analyzeTrainingSession } from '@/utils/strengthTrainingAnalyzer.js'
import { useSettings } from '@/stores/settings.js'
import { getActionCategory } from '@/utils/dayHelper.js'
import { useDayDataCache } from '@/stores/dayDataCache.js'

export default {
  name: 'training-analysis-card',
  props: {
    // 父组件 day.vue 传下来的原始引用，避免重复 deep copy
    entries: { type: Object, required: true },      // {动作名:[Entry]}
    hrSamplesWithTs: { type: Array, default: () => [] },
    currentDateStr: { type: String, required: true }, // YYYY-MM-DD
    profile: { type: Object, required: true },       // userProfile.toProfile()
    existingAnalysis: { type: Object, default: null }, // dayData.trainingAnalysis（可能已缓存）
  },
  data() {
    return {
      expanded: false,        // 折叠/展开
      setDetailExpanded: false, // 按组详情次级折叠
      analysis: null,         // 当前展示用分析结果（computed 驱动或 mounted 时首算）
      actionCategoryOf: (name) => getActionCategory(name),
    }
  },
  computed: {
    actionsArr() { return Object.entries(this.entries || {}) },
    hasEnoughData() {
      const entryCount = this.actionsArr.reduce((n, [_, list]) => n + (list ? list.filter(e => !e.isPlaceholder).length : 0), 0)
      return entryCount >= 2 && (this.hrSamplesWithTs?.length || 0) >= 10
    },
    headerMetrics() {
      if (!this.analysis) return { tvl: '—', avgHr: '—', peakHr: '—' }
      const s = this.analysis.session
      return {
        tvl: s.totalVolumeLoad.toLocaleString('en-US') + ' kg',
        avgHr: s.averageHr === null ? '—' : Math.round(s.averageHr),
        peakHr: s.peakHr === null ? '—' : s.peakHr,
      }
    },
    top3Actions() {
      if (!this.analysis) return []
      return Object.values(this.analysis.actionAnalyses)
        .sort((a, b) => b.totalVolume - a.totalVolume)
        .slice(0, 3)
    },
    sessionFinalized() { return this.analysis && this.analysis.status === 'finalized' },
  },
  watch: {
    // entries 或 HR 样本变化时自动重算（节流，setInterval 可不用——父组件 onConfirmEntry 后立即调用 this.$refs.tac.recalc()）
  },
  methods: {
    // 暴露给父组件调用的 API
    recalc(saveAfter = false) {
      // a) 构建 FlatEntry 排序列：entries 跨动作展开
      const flat = []
      for (const [aname, list] of Object.entries(this.entries || {})) {
        if (!Array.isArray(list)) continue
        let idx = 0
        for (const e of list) {
          if (e.isPlaceholder) continue
          idx += 1
          const w = e.stages?.[0]?.weight || 0
          const r = e.stages?.reduce((s, st) => s + (Number(st.reps) || 0), 0) || 0
          flat.push({
            actionName: aname, setIndex: idx,
            timestamp: e.timestamp || null,
            weight: w, reps: r, volumeLoad: w * r,
            stages: e.stages, bwMode: e.bwMode,
          })
        }
      }
      flat.sort((a, b) => (Number(a.timestamp) || 0) - (Number(b.timestamp) || 0))
      // b) restDurationSecByAction
      const s = useSettings()
      const restFn = (actionName) => {
        const cat = getActionCategory(actionName)
        if (cat === 'heavy') return Number(s.heavyTimerDuration) || 180
        return Number(s.lightTimerDuration) || 120
      }
      // c) historicalBestGetter：忽略 currentDateStr，调用 useDayDataCache 的扫描接口（若 store 无此方法 → 自行实现 10 天窗口扫描兜底）
      const cache = useDayDataCache()
      const histBest = (actionName) => {
        const fallback = cache.scanHistoricalBestForAction
          ? cache.scanHistoricalBestForAction(actionName, this.currentDateStr)
          : null
        return fallback
      }
      const result = analyzeTrainingSession({
        entries: flat,
        hrSamplesWithTs: this.hrSamplesWithTs,
        restDurationSecByAction: restFn,
        profile: this.profile,
        historicalBestGetter: histBest,
      })
      // status 保留 existingAnalysis（可能已 finalized）
      if (this.existingAnalysis?.status === 'finalized') {
        result.status = 'finalized'
        result.sessionEndTs = this.existingAnalysis.sessionEndTs || result.sessionEndTs
      }
      result.generatedAt = Date.now()
      this.analysis = result
      if (saveAfter) {
        cache.saveTrainingAnalysis(this.currentDateStr, result) // 若 dayDataCache 没这个方法，父组件接管（见 Task 12 Step 4）
      }
      return result
    },
    toggle() { if (this.hasEnoughData) this.expanded = !this.expanded },
    goFullReport() {
      uni.navigateTo({ url: `/pages/trainingAnalysis/trainingAnalysis?date=${this.currentDateStr}` })
    },
    finalizeSession() {
      if (!this.analysis || this.sessionFinalized) return
      this.analysis.status = 'finalized'
      const lastTs = this.analysis.setAnalyses.length
        ? Math.max(...this.analysis.setAnalyses.map(s => Number(s.timestamp) || 0))
        : 0
      this.analysis.sessionEndTs = Math.max(lastTs, Date.now())
      // 保存
      const cache = useDayDataCache()
      cache.saveTrainingAnalysis(this.currentDateStr, this.analysis)
    },
    sourceLabel(src) { return { profile: '用户填写', estimated: '估算值（初期60s）', formula: '公式 (220-age)', 'age-missing': '年龄未填' }[src] || src }
  },
  mounted() {
    // 首次用缓存；没有则计算（不立即 save，等用户点"立即重新计算"或父组件 onConfirmEntry 后 saveAfter=true）
    if (this.existingAnalysis) this.analysis = this.existingAnalysis
    else if (this.hasEnoughData) this.recalc(false)
  },
}
```

- [ ] **Step 2: template（折叠态 + 展开态三段 + 底部操作栏）**

```html
<template>
  <view class="tac-wrap">
    <!-- Header bar -->
    <view class="tac-header" :class="['tac-h', { 'disabled': !hasEnoughData, 'expanded': expanded }]" @click="toggle">
      <view class="tac-title">
        <text class="tac-icon">📊</text>
        <text class="tac-txt">训练分析</text>
      </view>
      <view class="tac-mini-metrics" v-if="hasEnoughData">
        <view class="tac-m"><span class="lbl">容量</span><span class="val">{{ headerMetrics.tvl }}</span></view>
        <view class="tac-m"><span class="lbl">平均♥</span><span class="val red">{{ headerMetrics.avgHr }}</span></view>
        <view class="tac-m"><span class="lbl">峰值♥</span><span class="val red">{{ headerMetrics.peakHr }}</span></view>
      </view>
      <view class="tac-mini-metrics" v-else><span class="tac-warn">（积累更多数据... 至少 2 组 + 10 个 HR 采样）</span></view>
      <text class="tac-arrow">{{ expanded ? '▴' : '▾' }}</text>
    </view>

    <!-- Body -->
    <view class="tac-body" v-if="expanded && analysis">
      <!-- S1: 双 KPI -->
      <view class="dual-kpi">
        <view class="kpi-card mech">
          <div class="kpi-label">Mechanical Training Load</div>
          <div class="kpi-value">{{ analysis.session.totalVolumeLoad.toLocaleString('en-US') }} <small>kg</small></div>
          <div class="kpi-sub">Total Volume · Σ weight × reps</div>
        </view>
        <view class="kpi-card cv">
          <div class="kpi-label">Cardiovascular Load</div>
          <div class="kpi-value">{{ analysis.session.cardiovascularLoad === null ? '—' : analysis.session.cardiovascularLoad }} <small>HRR·min</small></div>
          <div class="kpi-sub">独立指标 · HRR 梯形积分</div>
        </view>
      </view>
      <!-- S2: 三格指标 + 来源 -->
      <view class="tri-stats">
        <view class="tri-cell"><div class="lbl">Total Volume</div><div class="val">{{ analysis.session.totalVolumeLoad }} kg</div></view>
        <view class="tri-cell"><div class="lbl">Average HR</div><div class="val">
          <template v-if="analysis.session.averageHr !== null">{{ analysis.session.averageHr }} bpm</template>
          <template v-else>—</template>
        </div></view>
        <view class="tri-cell"><div class="lbl">Peak HR</div><div class="val">
          <template v-if="analysis.session.peakHr !== null">{{ analysis.session.peakHr }} bpm</template>
          <template v-else>—</template>
        </div></view>
      </view>
      <view class="src-row">
        restingHR: <b>{{ analysis.session.restingHrUsed.value ?? '—' }}</b>（{{ sourceLabel(analysis.session.restingHrUsed.source) }}）
        &nbsp;|&nbsp; maxHR: <b>{{ analysis.session.maxHrUsed.value ?? '—' }}</b>（{{ sourceLabel(analysis.session.maxHrUsed.source) }}）
      </view>

      <!-- S3: 动作 HR 对比（Top 3） -->
      <view class="act-sec-title">🏋 动作级别 HR 对比（本次训练）</view>
      <view class="act-list" v-if="top3Actions.length">
        <view class="act-row" v-for="(a, i) in top3Actions" :key="i">
          <div class="act-name">{{ a.actionName }}</div>
          <div class="act-meta-bar">
            <div class="act-meta">♥平均 {{ a.averageHr ?? '—' }} · ♥峰值 {{ a.peakHr ?? '—' }} · x/{{ a.analyzedSetCount }}/{{ a.setCount }}</div>
            <div class="act-bar-bg">
              <div class="act-bar-fill"
                   :style="{ width: (a.averageHr ? (a.averageHr - 60) * 0.9 + '%' : '2%') }"></div>
            </div>
          </div>
          <div class="act-delta" :class="'lv-' + (a.avgDeltaHr === null ? 0 : (a.avgDeltaHr >= 40 ? 3 : a.avgDeltaHr >= 25 ? 2 : 1))">
            {{ a.avgDeltaHr === null ? '—' : (a.avgDeltaHr > 0 ? '+' : '') + Math.round(a.avgDeltaHr) }}
          </div>
        </view>
      </view>
      <view class="tac-more" v-if="Object.keys(analysis.actionAnalyses).length > 3" @click="goFullReport">
        查看全部 {{ Object.keys(analysis.actionAnalyses).length }} 个动作 →
      </view>

      <!-- S4: 按组详情（默认折叠） -->
      <view class="set-detail-toggle" @click="setDetailExpanded = !setDetailExpanded">
        <text>{{ setDetailExpanded ? '收起按组详情' : '展开按组详情（逐组 HR 窗口）' }}</text>
        <text class="arr">{{ setDetailExpanded ? '▴' : '▾' }}</text>
      </view>
      <view class="set-detail-wrap" v-if="setDetailExpanded">
        <view class="set-row set-header">
          <span class="col">#</span>
          <span class="col">动作</span>
          <span class="col">w×r</span>
          <span class="col">VL</span>
          <span class="col">HR 窗口</span>
          <span class="col">ΔHR</span>
        </view>
        <view class="set-row" v-for="(s, i) in analysis.setAnalyses" :key="i">
          <span class="col">{{ i + 1 }}</span>
          <span class="col sm">{{ s.actionName }}</span>
          <span class="col">{{ s.weight }}×{{ s.reps }}</span>
          <span class="col">{{ s.volumeLoad }}</span>
          <span class="col" v-if="s.hr.analyzed">
            {{ s.hr.startHr }}→<b>{{ s.hr.peakHr }}</b>→{{ s.hr.endHr }}
            <small>({{ s.hr.sampleCount }}smp · est {{ s.estimatedSetDurationSec ?? '—' }}s)</small>
          </span>
          <span class="col warn" v-else>⚠ 采样不足{{ s.estimatedSetDurationSec === 0 ? '（窗口异常）' : '' }}</span>
          <span class="col" v-if="s.hr.analyzed && s.hr.deltaHr !== null">{{ s.hr.deltaHr > 0 ? '+' : '' }}{{ s.hr.deltaHr }}</span>
          <span class="col" v-else>—</span>
        </view>
      </view>

      <!-- Footer actions -->
      <view class="tac-footer">
        <text class="btn-link" @click="goFullReport">查看完整训练分析报告 →</text>
        <view class="footer-btns">
          <text class="btn-ghost" @click="recalc(true)">立即重新计算</text>
          <text class="btn-primary"
                :class="{disabled: sessionFinalized || !hasEnoughData}"
                @click="finalizeSession">
            {{ sessionFinalized ? '训练已结束' : '结束训练' }}
          </text>
        </view>
      </view>
    </view>
  </view>
</template>
```

- [ ] **Step 3: 样式（沿用现有组件风格 + 双KPI 钢蓝/红色左边框 + disabled 置灰）**

```css
<style lang="scss" scoped>
.tac-wrap { margin: 0 12px 16px; border-radius: 12px; background: var(--surface); border: 1px solid var(--border); overflow: hidden; }
.tac-header { display: flex; align-items: center; padding: 12px 16px; background: var(--surface-muted); border-bottom: 1px solid var(--border); }
.tac-header.disabled { opacity: 0.55; }
.tac-title { display: flex; align-items: center; gap: 8px; font: 600 16px/24px var(--font-sans); }
.tac-icon { font-size: 18px; }
.tac-mini-metrics { flex: 1; display: flex; gap: 16px; justify-content: flex-end; margin-right: 12px; font-size: 13px; }
.tac-m .lbl { color: var(--text-muted); font-size: 11px; margin-right: 4px; }
.tac-m .val { font-weight: 700; }
.tac-m .val.red { color: var(--chart-series-2, #ef4444); }
.tac-warn { color: var(--text-muted); font-size: 12px; }
.tac-arrow { color: var(--text-secondary); font-size: 12px; }
.tac-body { padding: 16px; }
.dual-kpi { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-bottom: 12px; }
.kpi-card { border-radius: 8px; padding: 12px; background: var(--surface-muted); border-left: 3px solid var(--border); }
.kpi-card.mech { border-left-color: var(--chart-series-1, #6366f1); }
.kpi-card.cv   { border-left-color: var(--chart-series-2, #ef4444); }
.kpi-label { font-size: 12px; color: var(--text-muted); }
.kpi-value { font: 700 18px/26px var(--font-sans); margin: 4px 0; }
.kpi-value small { font-size: 11px; font-weight: 500; color: var(--text-secondary); }
.kpi-sub   { font-size: 11px; color: var(--text-secondary); }
.tri-stats { display: grid; grid-template-columns: repeat(3,1fr); gap: 8px; padding: 12px 0; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
.tri-cell { text-align: center; }
.tri-cell .lbl { font-size: 11px; color: var(--text-muted); }
.tri-cell .val { font-size: 14px; font-weight: 700; margin-top: 2px; }
.src-row { padding: 6px 0 12px; font-size: 11px; color: var(--text-muted); }
.src-row b { color: var(--text-primary); font-weight: 600; }
.act-sec-title { padding: 6px 0 8px; font-size: 14px; font-weight: 600; }
.act-row { display: grid; grid-template-columns: 90px 1fr 50px; align-items: center; gap: 8px; padding: 6px 0; }
.act-name { font-size: 13px; font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.act-meta { font-size: 11px; color: var(--text-secondary); }
.act-bar-bg { height: 7px; background: var(--surface-muted); border-radius: 999px; border: 1px solid var(--border); margin-top: 4px; overflow: hidden; }
.act-bar-fill { height: 100%; background: linear-gradient(90deg, #6366f1, #ef4444); border-radius: 999px; transition: width .2s; }
.act-delta { font-size: 12px; font-weight: 700; text-align: right; }
.act-delta.lv-0 { color: var(--text-muted); }
.act-delta.lv-1 { color: var(--chart-series-4, #22c55e); }
.act-delta.lv-2 { color: var(--chart-series-3, #f59e0b); }
.act-delta.lv-3 { color: var(--chart-series-2, #ef4444); }
.tac-more { text-align: right; padding: 6px 0 10px; color: var(--brand, #6366f1); font-size: 12px; font-weight: 600; }
.set-detail-toggle { display: flex; justify-content: space-between; padding: 10px 0; border-top: 1px dashed var(--border); font-size: 12px; color: var(--text-secondary); }
.set-row { display: grid; grid-template-columns: 28px 1fr 70px 60px 1.2fr 60px; gap: 4px; padding: 6px 4px; font-size: 11px; border-bottom: 1px solid var(--border); }
.set-row .col { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.set-row.set-header { background: var(--surface-muted); font-weight: 600; color: var(--text-secondary); }
.set-row .col.sm { font-weight: 600; font-size: 12px; }
.set-row .col.warn { color: var(--chart-series-2, #ef4444); font-size: 10px; }
.set-row small { display: block; color: var(--text-muted); font-size: 9px; }
.tac-footer { margin-top: 16px; display: flex; justify-content: space-between; align-items: center; padding-top: 12px; border-top: 1px solid var(--border); }
.btn-link { color: var(--brand, #6366f1); font-size: 12px; font-weight: 600; }
.footer-btns { display: flex; gap: 8px; }
.btn-ghost { padding: 5px 12px; border-radius: 6px; border: 1px solid var(--border); font-size: 12px; }
.btn-primary { padding: 5px 14px; border-radius: 6px; background: var(--brand, #6366f1); color: #fff; font-size: 12px; font-weight: 600; }
.btn-primary.disabled { opacity: 0.4; }
</style>
```

- [ ] **Step 4: dayDataCache 补 saveTrainingAnalysis & scanHistoricalBestForAction 接口（需要 DayDataCache store 侧实现，若不存在则在 Task 12 Step 4 作为 dayDataCache 扩展实现）**

Read `stores/dayDataCache.js` 确认是否已有 `saveDayData(dateStr, patch)` 接口；若有，则在本组件可先降级为 `cache.saveDayData(currentDateStr, { trainingAnalysis: result })`，等待 Task 12 封装成专用 `saveTrainingAnalysis`。

- [ ] **Step 5: 手工验证（在 Task 12 day.vue 集成前，可用独立测试：写一个临时 Test 页引用组件，传 mock entries + 100+ mock HR samples + profile，观察折叠/展开/双KPI/动作对比/按组详情是否渲染正确）**

- [ ] **Step 6: Commit**

```bash
git add components/TrainingAnalysisCard.vue
git commit -m "ui(training-analysis): add TrainingAnalysisCard (foldable KPI card) — 双指标分离展示 + 动作 HR 对比 + 按组明细 + finalize/recalc"
```

---

## Task 10: HrBarChart.vue + HrProfileChart.vue（canvas 自绘，无 Chart.js 依赖）

两个组件都采用与 HeartRateChartPopup.vue 相同的 `双 canvas 模式（背景 + 前景数据）+ 透明 touch-overlay 视图做触摸交互`。

**Files:**
- Create: `pages/trainingAnalysis/components/HrBarChart.vue`（覆盖 Task 8 的占位符）
- Create: `pages/trainingAnalysis/components/HrProfileChart.vue`（覆盖 Task 8 的占位符）
- Style reference: Reuse `components/HeartRateChartPopup.vue`

### 子 Task 10.1: HrBarChart.vue（动作 HR 对比）

- [ ] **Step 1: 覆盖 Task 8 占位文件，写代码**

```javascript
// pages/trainingAnalysis/components/HrBarChart.vue
export default {
  name: 'hr-bar-chart',
  props: {
    // [{actionName, avgHr, peakHr, deltaHr}] —— 2-4 个动作
    series: { type: Array, required: true },
    width:  { type: Number, default: 340 },
    height: { type: Number, default: 240 },
  },
  data() { return { canvasSize: 2 /* dpr default, overwritten in mounted */ } },
  mounted() { this.$nextTick(() => { this.draw() }) },
  watch: { series: { deep: true, handler() { this.draw() } } },
  methods: {
    draw() {
      const info = uni.getSystemInfoSync()
      const dpr = info.pixelRatio || 2
      this.canvasSize = dpr
      const W = this.width * dpr, H = this.height * dpr
      const q = uni.createSelectorQuery().in(this)
      q.select('#hrbar-bg').fields({ node: true, size: true }).exec(([bg]) => {
        if (!bg) return
        const bgCanvas = bg.node
        bgCanvas.width = W; bgCanvas.height = H
        const ctxBg = bgCanvas.getContext('2d')
        ctxBg.scale(dpr, dpr)
        // 背景网格
        ctxBg.fillStyle = 'transparent'
        ctxBg.clearRect(0,0,W,H)
        const padL = 36, padR = 12, padT = 20, padB = 50
        const cw = this.width - padL - padR
        const ch = this.height - padT - padB
        const yMin = 40, yMax = 200
        const yToPx = (v) => padT + ch - (v - yMin) / (yMax - yMin) * ch
        // y 轴 参考线 40/80/120/160/200
        ctxBg.strokeStyle = '#e5e7eb'
        ctxBg.lineWidth = 0.5
        ctxBg.fillStyle = '#9ca3af'
        ctxBg.font = '10px sans-serif'
        for (let v = yMin; v <= yMax; v += 40) {
          const y = yToPx(v)
          ctxBg.beginPath(); ctxBg.moveTo(padL, y); ctxBg.lineTo(padL+cw, y); ctxBg.stroke()
          ctxBg.fillText(String(v), 6, y + 3)
        }
        // bars
        const n = this.series.length
        if (!n) return
        const groupW = cw / n
        const barW = Math.min(14, groupW * 0.22)
        for (let i = 0; i < n; i++) {
          const item = this.series[i]
          const gx = padL + i * groupW + groupW / 2
          if (item.avgHr !== null) {
            const x = gx - barW - 2
            const y1 = yToPx(item.avgHr), y2 = yToPx(yMin)
            ctxBg.fillStyle = '#6366f1'
            ctxBg.fillRect(x, y1, barW, Math.max(1, y2 - y1))
          }
          if (item.peakHr !== null) {
            const x = gx + 2
            const y1 = yToPx(item.peakHr), y2 = yToPx(yMin)
            ctxBg.fillStyle = '#ef4444'
            ctxBg.fillRect(x, y1, barW, Math.max(1, y2 - y1))
          }
          // x 轴 动作名（可能过长，截断）
          ctxBg.fillStyle = '#374151'
          ctxBg.font = 'bold 11px sans-serif'
          const label = item.actionName.length > 5 ? item.actionName.slice(0, 5) + '…' : item.actionName
          const tw = ctxBg.measureText(label).width
          ctxBg.fillText(label, gx - tw / 2, padT + ch + 14)
          // ΔHr 标签
          if (item.deltaHr !== null) {
            ctxBg.fillStyle = item.deltaHr >= 30 ? '#ef4444' : (item.deltaHr >= 20 ? '#f59e0b' : '#22c55e')
            ctxBg.font = 'bold 10px sans-serif'
            const dlabel = (item.deltaHr > 0 ? '+' : '') + Math.round(item.deltaHr)
            ctxBg.fillText(dlabel, gx - 10, padT + ch + 30)
          }
        }
        // 图例（右上角）
        ctxBg.fillStyle = '#6366f1'; ctxBg.fillRect(padL + cw - 110, 4, 8, 8)
        ctxBg.fillStyle = '#1f2937'; ctxBg.font = '10px sans-serif'
        ctxBg.fillText('平均HR', padL + cw - 98, 12)
        ctxBg.fillStyle = '#ef4444'; ctxBg.fillRect(padL + cw - 50, 4, 8, 8)
        ctxBg.fillStyle = '#1f2937'; ctxBg.fillText('峰值HR', padL + cw - 38, 12)
      })
    },
  },
}
```

```html
<template>
  <view class="hrbar-root" :style="{ width: width + 'px', height: height + 'px' }">
    <canvas id="hrbar-bg" type="2d" class="hrbar-canvas" :style="{ width: width + 'px', height: height + 'px' }"></canvas>
  </view>
</template>
<style scoped>
.hrbar-root { position: relative; background: var(--surface); border-radius: 8px; border: 1px solid var(--border); margin: 8px auto; }
.hrbar-canvas { width: 100%; height: 100%; }
</style>
```

- [ ] **Step 2: 手工验证**：在独立页传 2-4 个 mock 动作 → 看到 2 组柱（蓝平均HR / 红峰值HR）、Y 轴 40-200 刻度、ΔHR 文字 + 图例 ✓

- [ ] **Step 3: Commit HrBarChart**

```bash
git add pages/trainingAnalysis/components/HrBarChart.vue
git commit -m "ui(chart): add HrBarChart canvas component for action HR comparison (avg/peak bars + delta labels + legend)"
```

### 子 Task 10.2: HrProfileChart.vue（动作 HR Profile 散点 + 均值线）

- [ ] **Step 4: 覆盖占位文件**

```javascript
// pages/trainingAnalysis/components/HrProfileChart.vue
export default {
  name: 'hr-profile-chart',
  props: {
    profiles: { type: Array, required: true }, // aggregateActionHrProfiles() 返回值
    targetActionName: { type: String, required: true },
    width: { type: Number, default: 340 },
    height: { type: Number, default: 280 },
  },
  mounted() { this.$nextTick(() => this.draw()) },
  watch: { profiles: { deep: true, handler() { this.draw() } }, targetActionName() { this.draw() } },
  methods: {
    draw() {
      const info = uni.getSystemInfoSync()
      const dpr = info.pixelRatio || 2
      const W = this.width * dpr, H = this.height * dpr
      uni.createSelectorQuery().in(this).select('#hrpf-bg').fields({ node: true, size: true }).exec(([bg]) => {
        if (!bg) return
        const c = bg.node; c.width = W; c.height = H
        const ctx = c.getContext('2d'); ctx.scale(dpr, dpr)
        ctx.clearRect(0, 0, W, H)
        const padL = 36, padR = 12, padT = 20, padB = 70
        const cw = this.width - padL - padR, ch = this.height - padT - padB
        const buckets = this.profiles.filter(p => p.sampleSets > 0)
        if (!buckets.length) {
          ctx.fillStyle = '#9ca3af'; ctx.font = '12px sans-serif'
          ctx.fillText(`${this.targetActionName} · 暂无足够历史数据做 Profile`, padL + 20, padT + ch / 2)
          return
        }
        const xs = buckets.map(b => b.weightBucketLower)
        const xMin = Math.min(...xs, 20), xMax = Math.max(...xs, 100)
        const yMin = 40, yMax = 200
        const xToPx = (v) => padL + ((v - xMin) / (xMax - xMin || 1)) * cw
        const yToPx = (v) => padT + ch - ((v - yMin) / (yMax - yMin)) * ch
        // Y 轴网格
        ctx.strokeStyle = '#e5e7eb'; ctx.lineWidth = 0.5; ctx.fillStyle = '#9ca3af'; ctx.font = '10px sans-serif'
        for (let v = yMin; v <= yMax; v += 40) {
          const y = yToPx(v)
          ctx.beginPath(); ctx.moveTo(padL, y); ctx.lineTo(padL + cw, y); ctx.stroke()
          ctx.fillText(String(v), 6, y + 3)
        }
        // X 轴 weight 标签（斜放避免重叠）
        for (const b of buckets) {
          const x = xToPx(b.weightBucketLower)
          ctx.strokeStyle = '#9ca3af'; ctx.beginPath()
          ctx.moveTo(x, padT + ch); ctx.lineTo(x, padT + ch + 4); ctx.stroke()
          ctx.fillStyle = '#374151'
          ctx.save()
          ctx.translate(x - 2, padT + ch + 8)
          ctx.rotate(-Math.PI / 6)
          ctx.font = '10px sans-serif'
          ctx.fillText(b.weightBucket + 'kg', 0, 0)
          ctx.restore()
        }
        // 散点（所有 set 的 avgHr）
        for (const b of buckets) {
          const bx = xToPx(b.weightBucketLower)
          for (const p of b.points) {
            if (p.avgHr === null) continue
            // x 加随机抖动 ±6，避免点完全重叠
            const jitter = (Math.random() * 12 - 6)
            ctx.beginPath(); ctx.arc(bx + jitter, yToPx(p.avgHr), 3, 0, Math.PI * 2)
            ctx.fillStyle = 'rgba(99, 102, 241, 0.5)'; ctx.fill()
            ctx.strokeStyle = '#6366f1'; ctx.stroke()
          }
        }
        // 均值线（avgHr 蓝实线）+ peakHr 红虚线
        const drawLine = (propName, color, dashed) => {
          ctx.beginPath()
          let started = false
          for (const b of buckets) {
            const v = b[propName]
            if (v === null) continue
            const x = xToPx(b.weightBucketLower), y = yToPx(v)
            if (!started) { ctx.moveTo(x, y); started = true } else ctx.lineTo(x, y)
          }
          ctx.strokeStyle = color
          ctx.lineWidth = 2
          ctx.setLineDash(dashed || [])
          ctx.stroke()
          ctx.setLineDash([])
        }
        drawLine('avgHr', '#6366f1', [])
        drawLine('peakHr', '#ef4444', [5, 4])
        // 图例
        ctx.fillStyle = '#6366f1'; ctx.fillRect(padL + cw - 150, 4, 20, 2)
        ctx.fillStyle = '#1f2937'; ctx.font = '10px sans-serif'
        ctx.fillText('平均HR', padL + cw - 126, 12)
        ctx.strokeStyle = '#ef4444'; ctx.setLineDash([5, 4]); ctx.lineWidth = 2
        ctx.beginPath(); ctx.moveTo(padL + cw - 74, 5); ctx.lineTo(padL + cw - 54, 5); ctx.stroke()
        ctx.setLineDash([])
        ctx.fillStyle = '#ef4444'
        ctx.fillStyle = '#1f2937'; ctx.fillText('峰值HR', padL + cw - 50, 12)
        // 标题
        ctx.fillStyle = '#1f2937'; ctx.font = 'bold 12px sans-serif'
        ctx.fillText(this.targetActionName + ' · HR Profile（按重量分桶）', padL, 14)
      })
    },
  },
}
```

```html
<template>
  <view class="hrpf-root" :style="{ width: width + 'px', height: height + 'px' }">
    <canvas id="hrpf-bg" type="2d" class="hrpf-canvas" :style="{ width: width + 'px', height: height + 'px' }"></canvas>
  </view>
</template>
<style scoped>
.hrpf-root { position: relative; background: var(--surface); border-radius: 8px; border: 1px solid var(--border); margin: 8px auto; }
.hrpf-canvas { width: 100%; height: 100%; }
</style>
```

- [ ] **Step 5: 手工验证**：传 2 日 mock trainingAnalysis 聚合的卧推 profiles → 看到 x 轴 20/40/60/80kg 桶标签（斜放）、每个桶内散点（蓝圆带抖动 ±6）+ 蓝实线（avgHr）+ 红虚线（peakHr）+ 标题 + 图例 ✓

- [ ] **Step 6: Commit**

```bash
git add pages/trainingAnalysis/components/HrProfileChart.vue
git commit -m "ui(chart): add HrProfileChart canvas component (action HR profiles by weight buckets: scatter + avgHr line / peakHr dashed line / legend)"
```

---

## Task 11: pages/trainingAnalysis/trainingAnalysis.vue —— 独立完整页（3 Tabs）

覆盖 Task 8 的占位 trainingAnalysis.vue，重写完整实现。

**Files:**
- Create (overwrite placeholder): `pages/trainingAnalysis/trainingAnalysis.vue`
- Depends: Task 10 (HrBarChart + HrProfileChart), Task 3 (analyzer + aggregateActionHrProfiles), Task 2 (restingHR store)

- [ ] **Step 1: 完整实现代码**

```html
<template>
  <view class="ta-page">
    <!-- Top: date picker -->
    <view class="ta-top">
      <picker mode="date" :value="dateStr" fields="day" :end="todayStr" @change="onDateChange">
        <view class="date-picker-btn">
          📅 {{ dateStr }} <text class="chev">▾</text>
        </view>
      </picker>
      <view class="ta-session-status" v-if="analysis">
        <text class="st-chip" :class="{finalized: analysis.status === 'finalized'}">
          {{ analysis.status === 'finalized' ? '已结束' : '进行中' }}
        </text>
        <text class="gen-txt">分析于 {{ formatTime(analysis.generatedAt) }}</text>
      </view>
    </view>

    <!-- Empty state -->
    <view class="empty-state" v-if="!analysis">
      <view class="empty-icon">📊</view>
      <view class="empty-title">所选日期暂无训练分析结果</view>
      <view class="empty-sub">请先进入 {{ dateStr }} 的训练页，确认至少 2 组训练 + 10 个以上 BLE HR 采样后生成分析。</view>
      <view class="btn-primary-empty" @click="goToday">跳转到今日训练页 →</view>
    </view>

    <!-- Tabs -->
    <view class="ta-tabs" v-if="analysis">
      <view class="tab" :class="{active: tab===1}" @click="tab=1">本次训练</view>
      <view class="tab" :class="{active: tab===2}" @click="tab=2">动作 HR 对比</view>
      <view class="tab" :class="{active: tab===3}" @click="tab=3">动作 HR Profile</view>
    </view>

    <!-- Tab 1: 完整重现 TrainingAnalysisCard -->
    <view v-if="analysis && tab===1">
      <TrainingAnalysisCard
        :entries="entriesFlatRestore"
        :hr-samples-with-ts="hrSamplesWithTs"
        :current-date-str="dateStr"
        :profile="profile"
        :existing-analysis="analysis"
        ref="tac"
        :force-expanded="true"
      />
      <!-- 按组详情（独立页默认展开） -->
      <view class="ta-table-card" v-if="analysis && analysis.setAnalyses && analysis.setAnalyses.length">
        <view class="tt-title">📋 组级明细（默认展开）</view>
        <view class="tt-row tt-h">
          <span class="tt-col">#</span>
          <span class="tt-col">动作</span>
          <span class="tt-col">组号</span>
          <span class="tt-col">w×r</span>
          <span class="tt-col">容量</span>
          <span class="tt-col">相对强度</span>
          <span class="tt-col">transition</span>
          <span class="tt-col">估算时长</span>
          <span class="tt-col">start → peak → end HR</span>
          <span class="tt-col">ΔHR</span>
        </view>
        <view class="tt-row" v-for="(s, i) in analysis.setAnalyses" :key="i">
          <span class="tt-col">{{ i + 1 }}</span>
          <span class="tt-col">{{ s.actionName }}</span>
          <span class="tt-col">{{ s.setIndex }}</span>
          <span class="tt-col">{{ s.weight }}×{{ s.reps }}</span>
          <span class="tt-col">{{ s.volumeLoad }}</span>
          <span class="tt-col" v-if="s.relativeIntensity.value !== null">
            {{ (s.relativeIntensity.value * 100).toFixed(0) }}%
            <small v-if="s.relativeIntensity.e1RMEstimated">(e1RM{{ s.relativeIntensity.e1RM?.toFixed(1) }})</small>
          </span>
          <span class="tt-col" v-else>—</span>
          <span class="tt-col">{{ s.transitionTimeSec !== null ? s.transitionTimeSec + 's' : '—' }}</span>
          <span class="tt-col" v-if="s.estimatedSetDurationSec !== null">
            {{ s.estimatedSetDurationSec }}s<small> (estimated)</small>
          </span>
          <span class="tt-col" v-else>—</span>
          <span class="tt-col" v-if="s.hr.analyzed">{{ s.hr.startHr }}→<b>{{ s.hr.peakHr }}</b>→{{ s.hr.endHr }} <small>({{ s.hr.sampleCount }}样本)</small></span>
          <span class="tt-col warn" v-else>⚠ 采样不足</span>
          <span class="tt-col" v-if="s.hr.analyzed">{{ s.hr.deltaHr > 0 ? '+' : '' }}{{ s.hr.deltaHr }}</span>
          <span class="tt-col" v-else>—</span>
        </view>
      </view>
    </view>

    <!-- Tab 2: HR 对比 -->
    <view v-if="analysis && tab===2">
      <view class="ta-card">
        <view class="card-title">选择动作做 HR 对比（最多 4 个，至少 2 个）</view>
        <view class="chips">
          <view
            v-for="a in actionKeys"
            :key="a"
            class="chip"
            :class="{active: selectedActions.includes(a)}"
            @click="toggleAction(a)"
          >{{ a }}</view>
        </view>
        <view class="warn-small" v-if="selectedActions.length < 2">请至少选择 2 个动作以绘制对比图</view>
        <view class="warn-small" v-if="analysis.actionAnalyses">
          已选 {{ selectedActions.length }} / {{ Object.keys(analysis.actionAnalyses).length }}
        </view>
        <HrBarChart v-if="seriesForChart.length >= 2" :series="seriesForChart" :width="canvasW" />
      </view>
    </view>

    <!-- Tab 3: HR Profile -->
    <view v-if="tab===3">
      <view class="ta-card">
        <view class="card-title">选择动作查看长期 HR Profile</view>
        <picker mode="selector" :range="profileActionOptions" range-key="label" @change="onProfileActionChange">
          <view class="picker-btn">
            {{ profileActionLabel || '请选择动作' }} <text class="chev">▾</text>
          </view>
        </picker>
        <view class="warn-small" v-if="!histLoaded && selectedProfileAction">正在读取历史数据…</view>
        <HrProfileChart
          v-if="selectedProfileAction && profileBuckets.length > 0"
          :profiles="profileBuckets"
          :target-action-name="selectedProfileAction"
          :width="canvasW"
        />
        <view class="warn-small" v-else-if="selectedProfileAction && profileBuckets.length === 0">
          该动作暂无足够历史（需 ≥ 1 组 analyzed HR set）。
        </view>
      </view>
    </view>
  </view>
</template>

<script>
import TrainingAnalysisCard from '@/components/TrainingAnalysisCard.vue'
import HrBarChart from './components/HrBarChart.vue'
import HrProfileChart from './components/HrProfileChart.vue'
import { useDayDataCache } from '@/stores/dayDataCache.js'
import { useUserProfile } from '@/stores/userProfile.js'
import { aggregateActionHrProfiles } from '@/utils/strengthTrainingAnalyzer.js'

export default {
  name: 'training-analysis-page',
  components: { TrainingAnalysisCard, HrBarChart, HrProfileChart },
  data() {
    return {
      dateStr: '',
      todayStr: '',
      tab: 1,
      analysis: null,
      hrSamplesWithTs: [],
      entriesFlatRestore: {},   // {actionName: [Entry]} — 为了复用 TrainingAnalysisCard prop 格式，反向从 trainingAnalysis.setAnalyses 重建最少对象（含 timestamp）
      selectedActions: [],
      selectedProfileAction: '',
      profileBuckets: [],
      histLoaded: false,
      canvasW: 340,
    }
  },
  computed: {
    profile() { return useUserProfile().toProfile() },
    actionKeys() {
      if (!this.analysis?.actionAnalyses) return []
      return Object.keys(this.analysis.actionAnalyses).sort((a, b) => {
        const A = this.analysis.actionAnalyses[a].totalVolume
        const B = this.analysis.actionAnalyses[b].totalVolume
        return B - A
      })
    },
    seriesForChart() {
      if (!this.analysis) return []
      return this.selectedActions.map(name => {
        const a = this.analysis.actionAnalyses[name] || {}
        return {
          actionName: name,
          avgHr: a.averageHr ?? null,
          peakHr: a.peakHr ?? null,
          deltaHr: a.avgDeltaHr ?? null,
        }
      })
    },
    profileActionOptions() {
      // 从缓存所有索引日期里扫描出现过 trainingAnalysis 的所有动作名
      const cache = useDayDataCache()
      const names = new Set()
      const allDates = cache.getAllIndexedDates ? cache.getAllIndexedDates() : [this.todayStr]
      for (const d of allDates) {
        const dd = cache.getDayData(d)
        if (dd && dd.trainingAnalysis?.actionAnalyses) Object.keys(dd.trainingAnalysis.actionAnalyses).forEach(n => names.add(n))
      }
      return Array.from(names).sort().map(n => ({ label: n, value: n }))
    },
    profileActionLabel() {
      return this.selectedProfileAction || ''
    },
  },
  onLoad(options) {
    const today = new Date()
    const y = today.getFullYear()
    const m = String(today.getMonth() + 1).padStart(2, '0')
    const d = String(today.getDate()).padStart(2, '0')
    this.todayStr = `${y}-${m}-${d}`
    this.dateStr = options.date || this.todayStr
    this.loadDay()
    const info = uni.getSystemInfoSync()
    this.canvasW = Math.min(info.windowWidth - 32, 360)
  },
  methods: {
    loadDay() {
      const cache = useDayDataCache()
      const dd = cache.getDayData(this.dateStr) || {}
      this.analysis = dd.trainingAnalysis || null
      this.hrSamplesWithTs = dd.hrSamplesWithTs || []
      // 为复用 TrainingAnalysisCard，反向重建 minimal entries（不含 stages 细节，但足够 analyzer 内部重算）
      const restored = {}
      if (this.analysis?.setAnalyses) {
        for (const s of this.analysis.setAnalyses) {
          if (!restored[s.actionName]) restored[s.actionName] = []
          restored[s.actionName].push({
            isPlaceholder: false,
            bwMode: null,
            stages: [{ reps: s.reps, weight: s.weight, total: s.volumeLoad }],
            timestamp: s.timestamp,
          })
        }
      }
      // fallback 用原 entries（训练当天跳转到独立页时还有）
      for (const [aname, list] of Object.entries(dd.entries || {})) {
        if (!restored[aname]) restored[aname] = list
      }
      this.entriesFlatRestore = restored
      // 默认 Tab2 选中前 2 个动作（如存在）
      if (this.actionKeys.length >= 2) this.selectedActions = [this.actionKeys[0], this.actionKeys[1]]
      else this.selectedActions = [...this.actionKeys]
      // Tab3 默认动作
      if (!this.selectedProfileAction && this.profileActionOptions.length) {
        this.selectedProfileAction = this.profileActionOptions[0].value
        this.loadProfiles()
      }
    },
    onDateChange(e) { this.dateStr = e.detail.value; this.loadDay() },
    goToday() {
      uni.redirectTo({ url: '/pages/index/day?date=' + this.dateStr, fail: () => uni.navigateBack() })
    },
    toggleAction(name) {
      const idx = this.selectedActions.indexOf(name)
      if (idx >= 0) this.selectedActions.splice(idx, 1)
      else {
        if (this.selectedActions.length >= 4) {
          uni.showToast({ title: '最多选 4 个', icon: 'none' })
          return
        }
        this.selectedActions.push(name)
      }
    },
    onProfileActionChange(e) {
      const idx = Number(e.detail.value)
      this.selectedProfileAction = this.profileActionOptions[idx]?.value || ''
      this.loadProfiles()
    },
    loadProfiles() {
      if (!this.selectedProfileAction) return
      this.histLoaded = false
      this.profileBuckets = []
      const cache = useDayDataCache()
      const dates = cache.getAllIndexedDates ? cache.getAllIndexedDates() : []
      // 近 90 天，按时间升序
      dates.sort()
      if (dates.length > 90) dates.splice(0, dates.length - 90)
      const histList = []
      for (const d of dates) {
        const dd = cache.getDayData(d)
        if (dd?.trainingAnalysis) histList.push({ date: d, analysis: dd.trainingAnalysis })
      }
      this.profileBuckets = aggregateActionHrProfiles(histList, this.selectedProfileAction)
      this.histLoaded = true
    },
    formatTime(ts) {
      if (!ts) return ''
      const d = new Date(ts)
      return `${d.getMonth() + 1}/${d.getDate()} ${String(d.getHours()).padStart(2, '0')}:${String(d.getMinutes()).padStart(2, '0')}`
    },
  },
}
</script>

<style lang="scss" scoped>
.ta-page { padding: 12px 8px 40px; min-height: 100vh; background: var(--bg-page, #0b0b0f); color: var(--text-primary); }
.ta-top { display: flex; align-items: center; justify-content: space-between; padding: 4px 8px 12px; }
.date-picker-btn { padding: 6px 10px; border-radius: 999px; border: 1px solid var(--border); font-size: 13px; font-weight: 600; background: var(--surface); }
.chev { color: var(--text-muted); margin-left: 4px; font-size: 11px; }
.ta-session-status { display: flex; gap: 8px; align-items: center; }
.st-chip { padding: 2px 8px; border-radius: 999px; background: var(--surface-muted); color: var(--text-secondary); font-size: 11px; font-weight: 600; }
.st-chip.finalized { background: rgba(99, 102, 241, 0.15); color: var(--brand, #6366f1); }
.gen-txt { font-size: 10px; color: var(--text-muted); }
.empty-state { margin: 40px auto; max-width: 360px; text-align: center; padding: 32px 16px; background: var(--surface); border-radius: 12px; border: 1px dashed var(--border); }
.empty-icon { font-size: 40px; }
.empty-title { font-size: 16px; font-weight: 600; margin: 12px 0 4px; }
.empty-sub { font-size: 12px; color: var(--text-secondary); margin-bottom: 16px; line-height: 1.6; }
.btn-primary-empty { display: inline-block; padding: 8px 16px; border-radius: 999px; background: var(--brand, #6366f1); color: #fff; font-size: 13px; font-weight: 600; }
.ta-tabs { display: flex; gap: 6px; padding: 0 4px 12px; }
.tab { flex: 1; text-align: center; padding: 10px 0; background: var(--surface-muted); border-radius: 8px; font-size: 13px; font-weight: 600; color: var(--text-secondary); border: 1px solid var(--border); }
.tab.active { background: var(--brand, #6366f1); color: #fff; border-color: transparent; }
.ta-card { margin: 0 4px 16px; padding: 16px 12px; background: var(--surface); border: 1px solid var(--border); border-radius: 12px; }
.card-title { font-size: 14px; font-weight: 600; margin-bottom: 10px; }
.chips { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 8px; }
.chip { padding: 5px 12px; border-radius: 999px; background: var(--surface-muted); color: var(--text-secondary); border: 1px solid var(--border); font-size: 12px; font-weight: 600; }
.chip.active { background: var(--brand, #6366f1); color: #fff; border-color: transparent; }
.warn-small { font-size: 11px; color: var(--text-muted); margin: 4px 0; }
.picker-btn { display: inline-block; padding: 6px 12px; border-radius: 8px; background: var(--surface-muted); border: 1px solid var(--border); font-size: 13px; font-weight: 600; color: var(--text-primary); margin-bottom: 10px; }

/* Tab1 表格 */
.ta-table-card { margin: 0 8px 16px; padding: 12px 6px; background: var(--surface); border: 1px solid var(--border); border-radius: 12px; overflow-x: auto; }
.tt-title { padding: 2px 8px 10px; font-size: 13px; font-weight: 600; }
.tt-row { display: grid; grid-template-columns: 28px 72px 40px 60px 54px 80px 62px 80px 140px 54px; padding: 6px 4px; border-bottom: 1px solid var(--border); font-size: 11px; min-width: 680px; }
.tt-row.tt-h { background: var(--surface-muted); font-weight: 700; color: var(--text-secondary); }
.tt-col { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.tt-col small { color: var(--text-muted); font-size: 9px; display: block; line-height: 1.2; }
.tt-col.warn { color: var(--chart-series-2, #ef4444); }
.tt-col b { font-size: 12px; color: var(--chart-series-2, #ef4444); }
</style>
```

**注**：TrainingAnalysisCard 在此页为"强制展开" —— 可在组件中 `props.forceExpanded: Boolean, default: false`，若 true → mounted 时 `this.expanded = true` 并禁用点击折叠。在 Task 9 Step 1 中补充：

```javascript
// TrainingAnalysisCard.props 加一行
forceExpanded: { type: Boolean, default: false },
// mounted() 末尾加
if (this.forceExpanded) this.expanded = true
```

- [ ] **Step 2: 手工验证（3 个 Tab）**

Tab1：
- Expected: 日期选择 + 空态 或 3 段 KPI/KPI/动作对比，按组详情展开（estimated 旁边有 "(estimated)" 小字）
- Expected: 状态 chip（进行中/已结束）+ 分析生成时间

Tab2：
- Expected: 动作 chips（≥2）默认选中 top2；点选最多 4 → 下方柱状图（蓝色平均、红色峰值、ΔHR 三色、图例）

Tab3：
- Expected: 动作选择 picker（默认第一项）→ 下方 canvas 散点 + 蓝实线/红虚线 + 标题/图例；无数据时显示提示

- [ ] **Step 3: Commit**

```bash
git add pages/trainingAnalysis/trainingAnalysis.vue
git commit -m "feat(page): add trainingAnalysis page (3 Tabs: full report / action HR comparison chips + bar / HR profile picker + scatter chart)"
```

---

## Task 12: pages/index/day.vue —— 核心集成（移除 kcal/时长 + hrSamplesWithTs 写入 + TrainingAnalysisCard 接入 + 分析调用）

**前置工作**：先完整 Read day.vue，重点定位：
- (A) data() 中所有 kcal 相关：`hrKcalTotal`, `hrBaseDurationSec`, `totalCalories`, `netCalories`, `metValue`, `metActivityKey`
- (B) `import` 中 calorieEstimate / metEstimate 引用
- (C) `startHrAccumulate` 方法（每秒累积 kcal + duration）
- (D) `_saveHrToDayData` 方法（写 caloriesTotal / durationSec / heartRateAvg）
- (E) 模板中 `<HeartRateToggle :kcal-total="..." :duration-sec="..." />` props 传值
- (F) 模板底部 `<scroll-view>` 里 list-bottom-space 前插入 TrainingAnalysisCard
- (G) mounted 恢复逻辑（caloriesTotal / durationSec 读取 → 移除）
- (H) `onConfirmEntry` 方法（点确认一组后需调用 TrainingAnalysisCard.recalc(true)）

本 Task 共 7 步，每步为最小化原子修改，便于出错时回滚。

- [ ] **Step 1: 移除 data() 中的 kcal / duration 字段**

在 `data()` 返回对象中**整行删除**：
```javascript
hrKcalTotal: 0,
hrBaseDurationSec: 0,
totalCalories: 0,
netCalories: 0,
metValue: null,
metActivityKey: null,
```
同时**新增**：
```javascript
hrSamplesWithTs: [],
lastTrainingAnalysisSaveTs: 0,
```

- [ ] **Step 2: 移除 calorie/met 相关 import，新增 analyzer & 组件 import**

删除行：
```javascript
import { calcCaloriesByKeytel, estimateAvgHr } from '@/utils/calorieEstimate.js'
import { calcMetCalories, selectActivityKey } from '@/utils/metEstimate.js'
```
新增行：
```javascript
import TrainingAnalysisCard from '@/components/TrainingAnalysisCard.vue'
// 注意：analyzeTrainingSession 核心算法在 TrainingAnalysisCard 组件内部封装，day.vue 不直接依赖（保持 SRP）。
```
components 字段里加：`components: { ..., TrainingAnalysisCard },`（若已在全局自动注册则不用）。

- [ ] **Step 3: 改造 startHrAccumulate —— 移除 kcal 累计，改为每秒 push hrSamplesWithTs**

原代码大概为：
```javascript
startHrAccumulate() {
  this.stopHrAccumulate()
  this.hrAccumTimer = setInterval(() => {
    if (!this.hrConnected || this.hr <= 0) return
    this.hrBaseDurationSec += 1
    const k = calcCaloriesByKeytel(...)
    this.hrKcalTotal += k
    if (this.hrBaseDurationSec % 60 === 0) this._saveHrToDayData()
    // hrSamples 分钟采样
    ...
  }, 1000)
}
```
替换为（保留 hrSamples 旧格式写入的"每分钟一条"以兼容旧图表），移除所有 calories 计算行：

```javascript
startHrAccumulate() {
  this.stopHrAccumulate()
  this.hrAccumTimer = setInterval(() => {
    if (!this.hrConnected || this.hr <= 0 || this.hrPausedForNoHr) return
    // ★ 新增：每秒 hrSamplesWithTs 双轨写入
    this.hrSamplesWithTs.push({ hr: this.hr, ts: Date.now() })
    // ★ 保留：旧格式每分钟 1 条（兼容弹窗展示历史，不再用于新分析）
    if (this.hrAccumSecBucketIdx === undefined) this.hrAccumSecBucketIdx = 0
    this.hrAccumSecBucketIdx += 1
    if (this.hrAccumSecBucketIdx % 60 === 0) {
      const avgH = estimateAvgHr(this.hrSamples) // 保留，无副作用仅取展示值（如不存在此函数改用 this.hr）
      this.hrSamples.push({ hr: Math.round(this.hr), durMin: 1 })
    }
    // ★ 每 5 分钟自动保存一次（防崩溃丢 HR 采样 with ts）
    if (this.hrAccumSecBucketIdx % 300 === 0) {
      this._saveHrToDayData()
    }
  }, 1000)
}
```

- [ ] **Step 4: 改造 _saveHrToDayData —— payload 移除 caloriesTotal/durationSec，新增 hrSamplesWithTs + trainingAnalysis**

原 payload：
```javascript
payload.heartRateAvg = ...
payload.caloriesTotal = this.totalCalories
payload.durationSec  = this.hrBaseDurationSec
payload.hrSamples    = this.hrSamples
```
改为：
```javascript
payload.heartRateAvg = this.hrSamplesWithTs && this.hrSamplesWithTs.length
  ? Math.round(this.hrSamplesWithTs.reduce((s, x) => s + x.hr, 0) / this.hrSamplesWithTs.length)
  : (this.heartRateAvg || null)
// 不新写入 caloriesTotal/durationSec（兼容：旧字段已在对象里，留空保持原状）
payload.hrSamples = this.hrSamples
payload.hrSamplesWithTs = this.hrSamplesWithTs || []
// TrainingAnalysisCard 内部若没调用 save，在这里同步取已有 analysis 写入（如果 this.$refs.tac 存在）
if (this.$refs.tac && this.$refs.tac.analysis) {
  payload.trainingAnalysis = this.$refs.tac.analysis
}
```

DayDataCache 新增 `scanHistoricalBestForAction` 和 `saveTrainingAnalysis`（如果没有的话，在 Task 9 中降级为通用 `saveDayData(date, {trainingAnalysis})`）。如果 store 确实缺失，这里在 store 上用 patch 方式添加：
```javascript
// 加到 stores/dayDataCache.js 的 actions：
// a) scanHistoricalBestForAction(actionName, ignoreDateStr) { ... 忽略 ignoreDateStr；其余日遍历 entry.weight/reps 取 w×(1+r/30) 最大 }
// b) saveTrainingAnalysis(dateStr, analysis) {
//      const dd = getDayData(dateStr)
//      dd.trainingAnalysis = analysis
//      saveDayData(dateStr, dd)
//    }
```

- [ ] **Step 5: template 修改（2 处）**

5a. HeartRateToggle props 修改：
```html
<!-- 旧 -->
<HeartRateToggle ... :kcal-total="totalCalories" :duration-sec="hrBaseDurationSec" ... />
<!-- 新：props 保留（兼容）但不再传 kcal/duration 语义有效值，传 0 即可（组件已经 deprecated） -->
<HeartRateToggle ... :kcal-total="0" :duration-sec="0" ... />
```

5b. scroll-view 内 TrainingAnalysisCard 插入（在 `.save-row`（心率按钮行容器）之后，或 list-bottom-space 之前）：
```html
<TrainingAnalysisCard
  v-if="todayDayData"
  ref="tac"
  :entries="todayDayData.entries || {}"
  :hr-samples-with-ts="hrSamplesWithTs"
  :current-date-str="todayStr"
  :profile="userProfileStore.toProfile()"
  :existing-analysis="todayDayData.trainingAnalysis || null"
/>
```

- [ ] **Step 6: onConfirmEntry 结尾触发 recalc(true)（保存组后立即增量计算 + 持久化）**

在 `onConfirmEntry` 最后一次 `this.saveDay()` 之后加：
```javascript
// 训练分析：保存组后立即计算并持久化
this.$nextTick(() => {
  if (this.$refs.tac) this.$refs.tac.recalc(true)
})
```

- [ ] **Step 7: mounted/onShow 恢复逻辑同步**

原 mounted 恢复块读取 `raw.caloriesTotal` / `raw.durationSec` 的代码**直接删除**。
新增：
```javascript
this.hrSamplesWithTs = Array.isArray(raw.hrSamplesWithTs) ? raw.hrSamplesWithTs.slice() : []
// TrainingAnalysisCard 组件初始化时会自动从 existingAnalysis 读取 raw.trainingAnalysis
```

- [ ] **Step 8: onUnload 离开页面时 finalize 自动判定（spec §6）**

`onUnload` / `beforeUnmount` 末尾加（注意：**仅当未已 finalized，且已断开连接 > 5 min 自最后一组时**才自动 finalize，不阻断用户临时离开回来继续）：
```javascript
beforeUnmount() {
  // ...原有 cleanup...
  // 自动 finalize 判定
  if (this.$refs.tac?.analysis?.status === 'finalized') return
  const lastEntryTs = (() => {
    let m = 0
    for (const list of Object.values(this.todayDayData?.entries || {})) {
      for (const e of list || []) {
        if (Number(e.timestamp) > m) m = Number(e.timestamp)
      }
    }
    return m
  })()
  const fiveMinAgo = Date.now() - 5 * 60 * 1000
  if (!this.hrConnected && lastEntryTs > 0 && lastEntryTs < fiveMinAgo) {
    this.$refs.tac && this.$refs.tac.finalizeSession()
  }
}
```

- [ ] **Step 9: 集成手工验证（真机 + 微信 devtools）**

验证①：未开蓝牙 → 训练页打开 → 无 kcal 显示 → 确认 2 组 → TrainingAnalysisCard 置灰「积累数据...」✓
验证②：连接手环 → 等 10 秒 → 确认 2 组（间隔 30s+）→ 卡片显示容量/平均♥/峰值♥ → 点开折叠 → 双KPI正确 ✓
验证③：卡片点「结束训练」→ 变灰「训练已结束」→ 重新进入 day.vue → 读取仍然 finalize ✓
验证④：打开训练分析报告（TrainingAnalysisCard 底部按钮跳转）→ Tab1/2/3 渲染 ✓
验证⑤：打开旧训练日（8-28 格式）→ TrainingAnalysisCard 显示 "旧版格式无法逐组 HR 分析"，不报错 ✓
验证⑥：BodyProfilePopup 未填 restingHR → 训练页 analysis 来源显示「估算值（初期60s）」；填 65 后变为「用户填写」✓

- [ ] **Step 10: Commit（最大变更，尽量单独 commit）**

```bash
git add stores/dayDataCache.js pages/index/day.vue
git commit -m "integration(day.vue): remove kcal/duration accumulate; push hrSamplesWithTs per-sec; integrate TrainingAnalysisCard; auto finalize on unload; add scanHistoricalBest/saveTrainingAnalysis to dayDataCache"
```

---

## Task 13: trainingStat 兼容收尾（仅注释 + 轻量兼容文案）

**Files:**
- Modify（必要时，非强制）：`pages/trainingStat/trainingStat.vue` 或 `statUtil.js` 顶部注释

- [ ] **Step 1: 在 statUtil.js 顶部加注释**

```javascript
/**
 * 2026-08-30 note: 热量消耗统计（collectCalories / 消耗趋势）仅基于 dayData 历史 caloriesTotal 字段。
 * 自本日期起，训练页 day.vue 运行时**不再新写入** caloriesTotal/durationSec（旧数据保留、新训练日这些字段为 0 或缺失）。
 * 新的训练负荷指标（Mechanical/Cardiovascular）在训练分析页 (pages/trainingAnalysis) 展示。
 *
 * 这里保留统计页的热量展示用于兼容旧训练日数据，在论文"数据兼容"章节可作为迁移案例说明。
 */
```

- [ ] **Step 2: 如果 trainingStat 页面有"今日 kcal"提示，在数据 < 1 时做小兼容（非强制）**

若 trainingStat.vue 模板中有 `今日热量消耗：xxx kcal` 文案且值为 0，添加一个条件渲染：当 caloriesTotal < 1 时显示 `（新训练分析模式，见训练分析报告 →）`，链接指向 `/pages/trainingAnalysis/trainingAnalysis?date=today`。
（不强制，如果实现难度 > 10 分钟可跳过，仅 Step 1 注释即可。）

- [ ] **Step 3: Commit**

```bash
git add pages/trainingStat/statUtil.js
git commit -m "docs(stats): compatibility note — calories trend only reflects historical data (pre 2026-08-30); new analysis uses trainingAnalysis page"
```

---

## 实施计划自检

**1. Spec 覆盖率（逐需求核对）**

| 需求条目 | 对应 Task | 覆盖？ |
|---|---|---|
| 移除 kcal 计算 + 时长统计（方案 B：字段保留兼容） | T5/T6/T12 S1-S4/T4/T13 | ✅ |
| transitionTime = ts[i+1] - ts[i] | T3 (analyzeTrainingSession step 4) | ✅ |
| estimatedSetDuration = transition - rest，显式标注 estimated | T3 代码 + T9/T11 模板 `(estimated)` 文字 | ✅ |
| 窗口内 startHR/avgHR/peakHR/endHR/ΔHR，不足不填假值 | T3 extractHrWindow + calcHrMetrics analyzed=false 全 null | ✅ |
| 不用 RIR | 全 plan 未出现 RIR 相关实现 | ✅ |
| volumeLoad = w×r，totalVolumeLoad = Σ | T3 calcVolumeLoad + session.totalVolumeLoad | ✅ |
| 存在 1RM → relativeIntensity；不存在 → Epley 估算 e1RM 标记 estimated | T3 estimateE1RMEpley + e1RMEstimated:true | ✅ |
| 心血管负荷 = HRR 积分，不作为热量估算 | T3 calcCardiovascularLoad（HRR·min）+ UI 独立 KPI | ✅ |
| Mechanical / Cardiovascular 分别展示，不合并单一值 | T9 dual-kpi 卡片独立展示 | ✅ |
| 展示 5 项：Mechanical Load / CV Load / Total Vol / Avg HR / Peak HR | T9 S1 + S2 3 格 | ✅ |
| 动作级别：平均 HR / Peak HR / ΔHR / 不同动作 HR 对比 | T9 S3 + T11 Tab2 + HrBarChart | ✅ |
| 动作 HR Profile 长期（不同重量/次数下 HR response + 绘图）| T3 aggregateActionHrProfiles + T10 HrProfileChart + T11 Tab3 | ✅ |

**2. Placeholder 扫描：0 处（所有步骤代码块完整，命令 exact path/command）**

**3. Type/Prop/函数名一致性自检**：
- `analyzeTrainingSession` 返回结构与 `TrainingAnalysis` 数据结构一致（字段名 session/setAnalyses/actionAnalyses/hrProfiles）✅
- `restingHR` store 字段名与组件 `v-model="localRestingHr"` → `profile.restingHR` 链路一致 ✅
- `HrBarChart.series[i]` 与 `seriesForChart` 字段对齐（actionName/avgHr/peakHr/deltaHr）✅
- `HrProfileChart.profiles` 与 `aggregateActionHrProfiles` 输出对齐（weightBucket/lower/.../points）✅
- Task 12 S4 `saveTrainingAnalysis` 与 Task 9 `recalc(saveAfter=true)` 接口一致 ✅

**无问题未修复项。**
