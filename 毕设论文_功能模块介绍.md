# 系统主要功能模块介绍

本系统围绕健身训练记录的全生命周期管理，从前端小程序、后端服务到管理后台三个维度构建了完整的功能体系。以下选取十二个主要功能模块进行介绍。

**（1）用户认证模块**

该模块负责系统入口的身份验证与权限分发，支持微信授权登录与管理员账号密码登录两种方式。登录成功后由后端签发 JWT 令牌，前端将令牌存储于本地并在后续请求中通过请求头携带，实现无状态认证。令牌设置有效期限与刷新宽限期，过期后自动引导用户重新登录，保障账户安全。

**（2）训练日历模块**

作为小程序首页，该模块以月历视图为核心，直观展示用户每日训练状态。不同训练部位以颜色区分标注于对应日期格中，用户可长按标记休息日，点击任意日期即可跳转至当日训练详情。日历下方设纪念日展示区，记录用户训练里程碑，增强用户使用粘性与训练仪式感。

**（3）训练记录模块**

该模块是系统的核心业务模块，支持用户按动作逐组记录训练重量与次数。系统自动调取同动作的历史训练数据，在当前组数旁标注重量增减幅度，辅助用户直观感知训练进步。模块内置组间休息倒计时器，支持力量训练与有氧训练两种模式，有氧模式可记录心率、配速及 MET 代谢当量。训练记录支持弹窗内实时编辑与删除。

**（4）训练统计模块**

该模块面向训练数据的纵向分析，以可视化图表呈现用户的训练规律。通过肌肉群网格分布图展示各部位训练频率，辅以周度和月度训练量趋势对比折线图，帮助用户发现训练偏科问题，为调整训练计划提供数据依据。

**（5）年度总览模块**

该模块以全年时间跨度展示用户的训练概貌，通过日历热力图直观反映每日训练强度分布。页面同步展示年度累计训练天数、总组数、涉及部位数等汇总指标，为用户年终训练回顾提供全量数据支撑。

**（6）动作库管理模块**

该模块提供训练动作数据的分类管理能力。预设动作按胸、背、腿、肩、手臂等部位分类展示，用户可在预设库基础上自定义新增、编辑与删除动作。每个动作保留完整的历史训练记录，支持按动作维度回溯查看历次训练数据。

**（7）训练模板模块**

该模块面向训练计划的复用与分享需求，支持用户创建、编辑和管理个人训练模板。模板可按分化训练方案（如推拉腿、上下肢分离等）组织动作序列，查看详情后可一键启动训练。模块内设模板广场子功能，用户可浏览、搜索、按标签筛选社区共享模板，并支持模板分享与导入操作，导入时对重名模板提示覆盖或跳过。

**（8）数据备份模块**

该模块保障用户训练数据的安全性与可迁移性，支持 JSON 与 CSV 两种格式的数据导出与导入。导入时执行智能合并去重策略，避免重复记录覆盖。同时对接微信云开发能力，提供云端备份与恢复通道，备份记录以列表形式展示并支持内容预览。

**（9）消息通知与公告模块**

该模块负责系统运营信息的触达与用户互动闭环。小程序端提供用户个人通知列表与系统公告浏览功能，用户可查看公告详情与个人通知消息。管理后台端提供公告发布、编辑、删除与上下线控制功能，公告内容同步推送至小程序端用户通知列表。同时配套反馈建议子功能，用户可提交意见反馈并跟踪处理状态，管理员在后台查看反馈列表并进行回复处理，形成完整的用户沟通通道。

**（10）数据仪表盘模块**

该模块为管理后台首页，面向管理员提供平台运营数据的全局概览。以指标卡片展示注册用户数、训练记录总量、共享模板数等核心统计值，通过 ECharts 可视化呈现用户增长趋势折线图与训练部位分布饼图，支持管理员快速掌握平台运营态势。

**（11）用户与管理员管理模块**

该模块实现后台双维度的账号管理能力。用户管理支持分页查询用户列表、查看用户训练统计详情及启用禁用操作。管理员管理支持管理员账号的增删改查与密码重置，并集成 RBAC 角色菜单权限配置功能，通过权限树可视化分配各角色的菜单访问权限。

**（12）模板审核与公告管理模块**

该模块承担平台内容的审核与运营发布职能。模板审核功能面向用户共享的社区模板，管理员可查看模板内容并执行通过或驳回操作，保障社区内容质量。公告管理功能支持系统公告的发布、编辑、删除及上下线控制，公告内容同步推送至小程序端用户通知列表。
