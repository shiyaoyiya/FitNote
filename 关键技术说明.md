# 关键技术说明

## 一、系统架构技术

### 1. 前后端分离架构

本项目采用前后端分离架构，前端与后端独立开发、独立部署，通过 RESTful API 进行数据交互。

- **小程序前端**：基于 **uni-app + Vue 3 + Composition API** 开发，编译为微信小程序，使用 Pinia 进行状态管理与本地持久化
- **管理后台前端**：基于 **Vue 3 + Vite + Element Plus** 构建，提供管理员操作界面
- **后端服务**：基于 **Spring Boot 2.7.x + Java 17**，提供 RESTful API 接口

### 2. 三层架构

后端严格遵循 MVC 三层架构设计：

- **表示层（Controller）**：负责接收 HTTP 请求、参数校验、调用业务逻辑、返回统一响应格式（`Result<T>` 封装）
- **业务逻辑层（Service）**：实现核心业务逻辑，以 `Service` 接口 + `ServiceImpl` 实现类的形式组织
- **数据访问层（Mapper）**：基于 MyBatis-Plus 的 `BaseMapper` 接口，负责数据库 CRUD 操作

## 二、数据库技术

### 1. 关系型数据库 — MySQL

使用 **MySQL 8.0** 作为主数据库，采用 InnoDB 存储引擎，支持事务与外键约束。数据库设计涵盖用户表（`sys_user`）、管理员表（`sys_admin`）、角色菜单关联表（`sys_role_menu`）、共享模板表（`shared_template`）等十余张数据表，通过 ER 模型规划实体关系与外键约束。

### 2. 数据库设计要点

- **逻辑删除**：通过 MyBatis-Plus 全局配置 `logic-delete-field: deleted`，实现数据软删除，保障数据可追溯
- **自动填充**：通过 `MyMetaObjectHandler` 实现创建时间、更新时间等审计字段的自动填充
- **字符编码**：统一使用 `utf8mb4` 字符集，支持 emoji 等完整 Unicode 字符存储

## 三、持久层技术 — MyBatis-Plus

采用 **MyBatis-Plus 3.5.5** 作为 ORM 框架，在 MyBatis 基础上增强开发效率：

- **BaseMapper 通用 CRUD**：继承 `BaseMapper<T>` 即可获得 `insert`、`deleteById`、`updateById`、`selectById`、`selectPage` 等通用方法，减少 XML 编写
- **LambdaQueryWrapper**：使用 Lambda 表达式构造查询条件，编译期校验字段名，避免硬编码字符串
- **分页插件**：通过 `MyBatisPlusConfig` 注册 `PaginationInnerInterceptor`，实现物理分页查询
- **XML 映射**：复杂查询（如多表关联统计）通过 `mapper/**/*.xml` 编写 SQL，兼顾灵活性与可维护性

## 四、安全与权限技术

### 1. 身份认证 — Spring Security + JWT

- **Spring Security**：作为安全框架核心，通过 `SecurityConfig` 配置过滤器链，禁用 CSRF 与 Session，采用无状态（`STATELESS`）策略
- **JWT 令牌**：使用 **jjwt 0.11.5** 库签发 HS512 签名的 JWT，令牌中携带用户 ID、类型（普通用户/管理员）、角色、用户名等信息，有效期 2 小时，支持 7 天刷新宽限
- **JwtAuthFilter**：自定义 `OncePerRequestFilter` 拦截器，从 `Authorization: Bearer` 请求头中解析并验证 JWT，将认证信息写入 `SecurityContextHolder`，实现无状态身份认证
- **密码加密**：使用 `BCryptPasswordEncoder`（强度 10）对用户密码进行哈希存储，保障密码安全

### 2. 权限控制 — RBAC

实现 **基于角色的访问控制（RBAC）** 体系：

- **角色设计**：区分普通用户（`USER`）与管理员（`ADMIN`、`AUDITOR`），管理员通过 `sys_admin` 表管理，角色与权限菜单通过 `sys_role_menu` 关联表映射
- **URL 级权限**：在 `SecurityConfig` 中通过 `antMatchers` 配置 URL 访问规则，如 `/api/admin/**` 仅允许 `ADMIN` 角色访问，模板审核接口允许 `ADMIN` 和 `AUDITOR` 角色访问
- **方法级权限**：通过 `@EnableGlobalMethodSecurity(prePostEnabled = true)` 启用方法级注解，结合自定义 `CustomPermissionEvaluator` 实现细粒度的按钮级权限校验（如 `@PreAuthorize("hasPermission(null, 'admin:user:list')")`）
- **双主体认证**：通过 `DualUserDetailsService` 统一管理小程序用户与管理员两套认证体系，`DualUserPrincipal` 封装双类型用户身份

## 五、工程规范技术

### 1. 统一响应格式

通过 `Result<T>` 包装类统一 API 响应结构，包含 `code`（状态码）、`message`（提示信息）、`data`（业务数据），配合 `ResultCode` 枚举定义标准状态码，前后端约定一致。

### 2. 统一异常处理

通过 `@RestControllerAdvice` 注解的 `GlobalExceptionHandler` 实现全局异常捕获，统一处理业务异常（`BusinessException`）、参数校验异常、权限异常与未知异常，返回标准化错误响应，避免敏感堆栈信息泄露。

### 3. 参数校验

基于 **Spring Boot Starter Validation**（Hibernate Validator），通过 `@NotBlank`、`@NotNull`、`@Size` 等 JSR-303 注解对 DTO 参数进行声明式校验，校验失败由全局异常处理器统一返回字段级错误信息。

### 4. 版本控制

使用 **Git** 进行版本控制，项目托管于代码仓库，支持分支管理与协作开发。

## 六、前端关键技术

### 1. uni-app 跨平台开发

- 基于 **uni-app** 框架开发小程序前端，一次编写可编译为微信小程序、Android App 等多端应用
- 使用 **Vue 3 Composition API**（`setup`、`ref`、`reactive`、`computed`）组织组件逻辑
- 通过 **Pinia** 状态管理库 + `uni.setStorageSync` 实现训练数据的本地持久化与全局状态共享

### 2. 管理后台技术栈

- **Vue 3 + Vite**：构建工具链，支持热更新与快速编译
- **Element Plus**：UI 组件库，提供表格、表单、对话框等管理后台常用组件
- **ECharts**：数据可视化库，用于仪表盘页面的训练统计趋势图、用户分布图等图表展示
- **Axios**：HTTP 客户端，封装请求拦截器统一附加 JWT 令牌与错误处理

### 3. 微信云开发

通过微信小程序云开发能力实现用户数据云端备份与恢复，支持 JSON/CSV 格式的数据导入导出与智能合并去重。

## 七、测试与部署技术

### 1. 测试

- **单元测试**：基于 **JUnit 5 + Spring Boot Test**，编写 `AuthFlowTest`（认证流程测试）、`BackupStatsExtractorTest`（备份数据解析测试）等测试类，验证核心业务逻辑正确性
- **接口测试**：通过 **Postman** 对 RESTful API 进行功能与边界测试，验证参数校验、权限控制与异常响应

### 2. 部署

- **后端部署**：通过 **Maven** 打包为可执行 JAR 文件（`fitnote-server-1.0.0.jar`），使用 `java -jar` 命令直接运行，内嵌 Tomcat 服务器
- **管理后台部署**：通过 Vite 构建为静态资源，可部署至 Nginx 或 GitHub Pages 等静态托管服务
- **小程序部署**：通过 HBuilderX 编译打包后上传至微信开放平台审核发布
